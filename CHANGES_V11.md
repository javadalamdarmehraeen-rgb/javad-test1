
## 11.89.0 — ۱۴۰۵/۰۶/۰۶
- رفع اخطار کنسول «نسخه SW متفاوت است» با هم‌نسخه کردن سرویس‌ورکر
- پشتیبان سرور هر ۱۵ دقیقه (به‌جای روزانه)
- یک جفت ویرایش/حذف در تعریف مسیر + فاصله فشرده داشبورد و مسیر

## 11.88.0 — ۱۴۰۵/۰۶/۰۶
- یک جفت کلید ویرایش/حذف در تب تعریف مسیر نمایندگان
- صفحه ورود دوباره فعال: `/` = لاگین، `/panel` = برنامه بعد از ورود نشست
- پرش خودکار ۷روزه از صفحه ورود حذف شد

## 11.87.0 — ۱۴۰۵/۰۶/۰۶
- توقف لرزش تب تعریف مسیر نمایندگان
- حذف فیلدهای تکراری پیام‌رسان در اطلاعات مجاز ارسال
- به‌روزرسانی تب تغییرات در نسخه جدید برای هر نسخه
- رفع هشدار دروغین «ورود فعال نیست» در عیب‌یابی
- پشتیبان ابری روزانه روی سرور (۱۴ روز) به‌عنوان پشتیبان اصلی چنددستگاهی
- بازگردانی کادر جمع تارگت همه پخش‌ها
- قفل چیدمان مرخصی + لیبل از ساعت/تا ساعت بدون ثانیه
- حذف فقط نمونه‌های شناخته‌شده؛ داروخانه کاربر خالی نمی‌شود
# 11.77.0 — ۱۴۰۵/۰۶/۰۵
- تب قیمت‌گذاری کالاها: قیمت فعلی، قیمت جدید، درصد افزایش، اعمال در تاریخ شمسی

# 11.76.0 — ۱۴۰۵/۰۶/۰۵
- آرشیو فایل اسنپ و دیتابیس پخش دیگر با نسخه جدید پاک نمی‌شود؛ حذف فقط با دستور مدیر
- کادر Autofill/passwords روی استان، شهر و منطقه حذف شد (select بومی)
- نام داروخانه مثل نام پزشک بدون زیرمجموعه
- هیت‌مپ فعالیت لحظه‌ای برای همه نمایندگان و به‌تفکیک نماینده

# نسخه ۱۱.۲۳.۰

1. Shafaarad code-first map 1001..1007 ثبت؛ 1005→1391911006 و 1006→1391911005 تست شد؛ scientific code normalization/row scan فعال.
2. کاربران: userEditId و Save Info واقعی، auth sync، role dropdown rep/sales/supervisor، permission checklist real checkbox per group/item.
3. Preset bar فقط first level select + Save Level؛ user/apply controls حذف؛ templates در settings و create-role auto apply.
4. server state file از server-db.json به gitignored user-data.json مهاجرت؛ Git/ZIP آن را لمس نمی‌کنند.
5. safeOrderFields اتمی برای form/list، capture intercept order inputs/buttons، بدون applyFullFormLayout و بدون تغییر visibility.
6. tests 18/18 سبز.

# نسخه ۱۱.۲۲.۳

1. Shafaarad code map اضافه: 1001→1391902001، 1002→1391911002، 1003→1391911003، 1004→1391911004، 1005→1391911006، 1006→1391911005، 1007→1391911007.
2. canonicalProduct برای Shafa code-first شد؛ inventory header «کالا» به‌عنوان code fallback شناخته می‌شود.
3. program table sticky headers gray/black؛ Excel Header #D1D5DB black، Total row red، percent text، number #,##0، borders.
4. fixed grid MutationObserver اضافه و list order exact table IDs/body observer/paneId/fuzzy/reliable rewrap شد.
5. address Iran-first کامل‌تر، postcode حذف؛ GPS watch best accuracy <=15m، opacity disabled، no fake.
6. test Shafa swapped woman/man codes، address no postcode، Excel styles، list observer. 17/17 سبز.

# نسخه ۱۱.۲۲.۲

1. آدرس reverse geocode فارسی از ایران→استان→شهرستان→بخش→شهر→منطقه→محله→خیابان→پلاک→کد پستی مرتب شد؛ display_name معکوس دیگر override نمی‌کند.
2. DOM field order lock مستقل اضافه: beforeunload capture، manager change capture، restore بعد layout engine/reliable boot.
3. Shafaarad schema قطعی: date7, sales8, return10, customer4, invoice6؛ monetary quantities×master prices؛ gift fields zero.
4. ستون دائمی inventory index10 «جمع تعداد موجودی» = index6 + index8، در هر import/restore بازسازی و در report qty استفاده می‌شود.
5. textarea address guard، GPS six decimals/maxAge0/no fake حفظ.
6. tests 16/16 با fixture Shafa و address order.

# نسخه ۱۱.۲۲.۱

1. همه digits دیداری و input غیرpassword به Latin تبدیل می‌شوند؛ grouping en-US است، تاریخ/کد/تلفن بدون grouping ولی Latin.
2. Excel SpreadsheetML styles اضافه: border چهارطرفه، Header، Number #,##0 و PercentText با % رشته‌ای.
3. sticky thead سراسری + max-height/overflow table-responsive برای freeze سرستون.
4. لرزش Product Info با شرط تغییر واقعی textContent رفع شد.
5. address inputs به textarea تبدیل، click/focus guard و reverse display_name کامل‌تر؛ GPS precision 6 decimals, maxAge0, timeout20s, بدون fake location.
6. list ordering getUnifiedFieldList(paneId)، fuzzy label match، matched>=1 و reliable rewrap اصلاح شد.
7. suite 15/15 سبز.

# نسخه ۱۱.۲۲.۰

1. schema واقعی موجودی دایا از نمونه کاربر ثبت شد: [کالای در راه، موجودی ریالی، موجودی، شعبه، بچ، انقضا، نام کالا، کد کالا، عنوان، ردیف].
2. invSchema exact-header-first شد تا «کالای در راه» دیگر به‌اشتباه نام کالا نشود؛ موجودی exact index2، نام exact index6، کد exact index7 در نمونه.
3. unit test واقعی ردیف 1112002/qty6 اضافه و سبز شد؛ total rows بدون code نادیده گرفته می‌شوند و شعب کد یکسان جمع می‌شوند.
4. suite 14/14 سبز.

# نسخه ۱۱.۲۱.۹

1. header alignment خودکار فایل موجودی دایا اضافه: leading one-cell offset با first-nonempty/length تشخیص و فقط header shift می‌شود، data دست‌نخورده.
2. backupهای منسوخ localStorage در load پاک و rolling save v15/app no-op شد تا quota مانع ذخیره current metadata نشود.
3. ترتیب metricRows از state.products است، نه alphabetic Object.keys.
4. SpreadsheetML پنج شیت، period metadata و ۱۹ ستون/rows ordered/master/total تثبیت شد.
5. test alignment inventory + product order اضافه؛ suite ۱۳/۱۳. DOM filter actual flex row nowrap, 5 children, correct parent, zero errors.

# نسخه ۱۱.۲۱.۸

1. Daya schema قطعی شد: sales=col5, gift=col4, returnProduct=col8, returnGift=col7, customer=col22, invoice=col13, date=col14, code=col16, inventory=col3.
2. همه ریال‌های دایا فقط quantity × master product price هستند؛ فایل ریال نادیده گرفته می‌شود. درصد gift/sales، return/sales، returnGift/returnProduct اصلاح شد.
3. per-product customer/invoice sets و total global union sets اضافه شد؛ جمع کل دیگر sum ساده per-product counts نیست.
4. customer فقط row با qty>0؛ invoice unique per product و full-return row حذف می‌شود.
5. fixed filter groups از cfHost به distributorFilterGrid اصلی بازگردانده و flex nowrap شد. DOM test: display flex, row nowrap, 5 children, correct parent, zero errors.
6. unit tests به ۱۲/۱۲ رسید: formula و global unique totals افزوده شد.

# نسخه ۱۱.۲۱.۷

1. تبدیل تاریخ دیتابیس پخش از normSnappDate به slashOnlyPersianDate تغییر کرد: فقط ۸ رقم شمسی اسلش می‌گیرد؛ 39694 و هر مقدار دیگر دست‌نخورده می‌ماند.
2. distLastDate و distDatePass و viewer پخش همگی slash-only شدند؛ تبدیل Excel serial/Gregorian فقط برای اسنپ باقی ماند.
3. فیلترهای پخش در desktop >=769 با flex row nowrap و min-width 180 در یک سطر اجباری شدند؛ اسکرول داخلی در کمبود عرض.
4. unit test دقیق 14050819→1405/08/19، 1405/08/19 unchanged و 39694 unchanged اضافه شد. ۱۰/۱۰ سبز.

# نسخه ۱۱.۲۱.۶

1. خطای runtime گزارش‌شده `rowSignature: (r||[]).map is not a function` رفع شد؛ normalizeStoredRow از Array/Object numeric/cells/row/data/JSON string پشتیبانی می‌کند.
2. snappStore همه legacy rows را پیش از filter/render به آرایه استاندارد migrate می‌کند؛ findHeaderRow و distLastDate نیز normalize می‌کنند.
3. unit test object row/cells و DOM runtime با state قدیمی واقعی اجرا شد: ۴ handler فعال، card render، snapp rows حفظ، صفر error.
4. setupهای اسنپ/پخش/کالا synchronous + reliable retry باقی و خطای قدیمی دیگر زنجیره را متوقف نمی‌کند.
5. viewerهای DB contenteditable و ذخیره batch→master→IndexedDB شدند.
6. عنوان product info از label DOM/dictionary تثبیت شد.

# نسخه ۱۱.۲۱.۵

1. تست واقعی DOM با jsdom اجرا و ریشه عدم اجرای امکانات پیدا شد: setup اولیه قبل از loadState کامل اجرا می‌شد و خطاها داخل try/catch پنهان بودند.
2. bind دکمه‌های import و CRUD کالا synchronous شد؛ reliableFeatureBoot روی click تب و load retry اجرا می‌شود. DOM test اتصال ۴ handler و render کارت پخش را تأیید کرد.
3. viewer دیتابیس اسنپ/پخش contenteditable و دارای ذخیره مستقیم IndexedDB/state/report شد؛ batch edit به master rows هم sync می‌شود.
4. distLastDate آخرین سطر فیزیکی ستون ۱۴، append pharmacy و label آخرین تاریخ حفظ شدند.
5. product info title با label واقعی DOM/dictionary اصلاح شد.
6. تست‌ها ۱۰/۱۰ + DOM runtime test بدون error.

# نسخه ۱۱.۲۱.۴

1. عنوان آخرین فایل به «آخرین تاریخ داروخانه/موجودی» تغییر و تاریخ دقیقاً از آخرین سطر غیرخالی (دایا ستون ۱۴) خوانده می‌شود؛ fallback فقط در نبود تاریخ است.
2. ورود فایل داروخانه append-only با concat(fresh) و dedupe باقی ماند و تست مستقیم last-row/append اضافه شد.
3. filter grid پخش در desktop پنج‌ستونه اجباری و در عرض‌های کمتر responsive شد؛ form-groupها grid-column auto دارند.
4. product code cell با data-no-number-group و تاریخ ۸رقمی از formatter مستثنی شد؛ input password/code نیز بدون گروه‌بندی است.
5. labelهای import اسنپ به button واقعی تبدیل و handler مستقل click/change با status «در حال خواندن» اضافه شد.
6. تست‌ها به ۱۰/۱۰ رسیدند.

# نسخه ۱۱.۲۱.۳

1. تمام بازیابی/ادغام خودکار rolling backup، latest backup، historical archive و remote GET حذف شد.
2. `CRM_APP_STATE_V2` فعلی تنها مرجع خودکار است؛ آرایه خالی حذف عمدی محسوب و خالی می‌ماند.
3. سرور فقط snapshot فعلی را با POST دریافت می‌کند و هرگز local را با GET/reload بازنویسی نمی‌کند.
4. mergeStateWithoutLoss از runtime حذف شد تا تصادفی دوباره فراخوانی نشود؛ backupها فقط بازیابی دستی‌اند.
5. unit tests بازنویسی شدند: old backup نباید user/pharmacy/doctor/product یا layout حذف‌شده را برگرداند؛ sample defaults هم نباید تزریق شوند. ۹/۹ سبز.
6. قانون دائمی ۷۱ ثبت شد.

# نسخه ۱۱.۲۱.۲

1. داده‌های حجیم Excel از localStorage به IndexedDB `crmBulkData` منتقل شدند؛ metadata سبک در localStorage و bulk در IDB است، با migration/restore خودکار پس از refresh.
2. backup تا آماده‌شدن bulk متوقف و server state فقط metadata سبک می‌گیرد؛ import سفر/شارژ/پخش قبل از save، bulk vault را await می‌کند.
3. کد کالا قبل نام کالا اضافه؛ seed 1001..1007 و فرمول `dayaDbCode=1111000+productCode` برای 1008→1112008 آینده. ستون ۱۶ دایا معیار تطبیق canonical است.
4. حذف کالا از products، salesTargets، orders و draft سفارش انجام و گزارش دایا کالای بدون mapping ثبت‌شده را نشان نمی‌دهد.
5. آخرین تاریخ فایل با ستون ۱۴ دایا و fallback زمان import؛ viewer تاریخ‌ها را اسلش‌دار می‌کند.
6. تب فروش پخش compact grid شد؛ گزارش به ۱۹ ستون با مرجوعی جایزه و موجودی ریالی پخش/داروخانه ارتقا یافت.
7. Daya mapping و محاسبات قیمت/درصد/pharmacy unique/invoice full-return اصلاح و جدول‌ها border کامل شدند.
8. تست IndexedDB metadata و product code اضافه؛ suite اکنون ۹/۹ سبز است.

# نسخه ۱۱.۲۱.۱

1. آرشیو قدیمی اسنپ با signature حذف trailing empty و ذخیره پاک‌سازی شد؛ تست عدد فارسی/ریال و dedupe سبز است.
2. کلیدهای مشاهده کل دیتابیس و هر batch سفر/شارژ اضافه و ماه با grey-zone کامل واقعاً disabled شد.
3. CRUD نهایی کالا در v20: افزودن/ویرایش/حذف، کلید کالای جدید و refresh کاتالوگ/سفارش.
4. تب «دیتابیس پخش‌ها» اضافه و همه مشاهده دیتابیس/موجودی/ریز فایل‌ها از فروش به آن منتقل شد؛ تاریخ نمایش نرمال اسلش‌دار است.
5. گزارش پخش به ۱۹ ستون (نام + ۱۸ metric) شامل مرجوعی جایزه و دو موجودی ریالی ارتقا یافت؛ نام کالا canonical و قیمت‌های خالی از قیمت برنامه محاسبه می‌شوند.
6. نگاشت دایا: تاریخ ۱۴، فاکتور ۱۳، فروش تعدادی ۵، ریال پخش ۴، ریال داروخانه ۸، جایزه ۳، موجودی ۳.
7. uniqueness داروخانه بر نام/استان/شهر/منطقه/آدرس و عدم شمارش فاکتور برگشت کامل اعمال شد؛ همه جداول خط‌کشی کامل شدند.
8. تست‌های ضد‌رگرسیون به ۸ مورد حفظ و با تب دیتابیس/CRUD/borders توسعه یافت.

# نسخه ۱۱.۲۱.۰

1. دو تست Node بدون dependency اضافه شد: unit regression حفظ داده/چیدمان/quantity/dedupe و automated HTTP smoke برای health/index/همه scriptها/API state.
2. `npm test` به GitHub Actions اضافه شد و شکست تست deployment را قرمز/متوقف می‌کند.
3. جمع عجیب اسنپ با dedupe خودکار آرشیو قبلی و تست واقعی parser اعداد فارسی/ریال اصلاح شد؛ مشاهده دیتابیس و فایل‌های سفر/شارژ اضافه شد.
4. تب «اطلاعات شرکت‌ها» برای دایا دارو، شفاآراد، تیوان و مشاطب با URL/username و password نشست امن اضافه شد.
5. تب «اطلاعات فروش پخش‌ها»: بازکردن پنل، آرشیو append داروخانه، replace موجودی، تاریخ آخرین فایل، مشاهده دیتابیس و فایل‌ها.
6. فیلتر year/month/day/range، dedupe کل ردیف، گزارش ۱۵ ستونی همه/هر پخش، جمع کل و اکسل SpreadsheetML چندشیتی فارسی اضافه شد.
7. دو فایل تست به OFFICIAL_FILELIST افزوده و قانون دائمی ۷۰ ثبت شد.

# نسخه ۱۱.۲۰.۵

1. تنظیمات/چیدمان یک‌بار از آخرین snapshot پیش از مهاجرت ۱۱.۲۰.۴ (مبنای ۱۱.۲۰.۳) بازگردانده و داده‌های همان snapshot دوباره union می‌شوند.
2. پس از بازیابی، marker مانع هر ادغام ساختاری خودکار بعدی است؛ layout جاری و تغییر مدیر مبنا می‌ماند.
3. mobile responsive، checkboxهای تک‌حالت اسنپ، وابستگی سال/ماه، dedupe کل ردیف و قانون تاریخ از ۱۱.۲۰.۴ روی مبنای ۱۱.۲۰.۳ حفظ شدند.
4. منطق range با خاموش‌کردن یکی از دو تیک، هر دو را خاموش می‌کند؛ خالی‌شدن سال، ماه و تیک ماه را پاک/قفل می‌کند.
5. تست مستقل بازگردانی layout ۱۱.۲۰.۳ و بازیابی هم‌زمان کاربر/داروخانه/پزشک/کالا موفق شد.

# نسخه ۱۱.۲۰.۴

1. نمای موبایل زیر ۷۶۸px بازطراحی شد: nav افقی مخفی، فقط همبرگر، فرم تک‌ستون، جدول اسکرول داخلی و بدون overflow صفحه.
2. سفر و شارژ هرکدام checkboxهای سال/ماه/از/تا با حالت انحصاری year/month/range دارند؛ range دو تیک تاریخ را جفت می‌کند و ماه تا ورود سال قفل است.
3. dedupe ردیف اکسل با امضای نرمال همه سلول‌ها برای سفر و شارژ تثبیت شد.
4. همه فیلدهای تاریخ متنی بر اساس ID/label به تقویم شمسی و اسلش خودکار متصل می‌شوند؛ سال/ماه/رمز مستثنی‌اند.
5. بازیابی تاریخی فقط یک‌بار union می‌شود؛ بعد از آن تغییر/حذف مدیر از backup قدیمی برنمی‌گردد. layout جاری همیشه مبناست و remote فقط یک‌بار merge می‌شود.
6. mirror خودکار ساختار در startup حذف شد؛ تغییر تعداد/جای فیلد و کادر فقط با اقدام مدیر.

# نسخه ۱۱.۲۰.۳

1. بازیابی از «انتخاب یک snapshot» به ادغام بدون حذف همه snapshotها تغییر کرد؛ کاربران، داروخانه‌ها، پزشکان، کالاها و سایر رکوردها union می‌شوند.
2. تمام آرشیوهای CRM_APP_STATE_* نیز بررسی و چیدمان کامل‌تر برای formFieldMeta/formBoxes/manualLayouts/tabOrder برنده می‌شود.
3. وضعیت سرور با وضعیت محلی ادغام می‌شود و دیگر حتی نسخه جدیدتر ولی کم‌اطلاعات، محلی را جایگزین نمی‌کند.
4. تطبیق نام اسنپ برای آقا/آقای/اقا/خانم/جناب/سرکار و تطبیق جزئی اصلاح شد.
5. نوار ثابت اسنپ آخرین تاریخ واقعی فایل سفر و افزایش موجودی را نشان می‌دهد و کلیدها بازخورد/اسکرول واضح دارند.
6. تراز ارتفاع لیبل/فیلد فرم‌های اصلی بدون تغییر order ذخیره‌شده تثبیت شد.

# نسخه ۱۱.۲۰.۲

1. نوار ثابت کلیدهای گزارش اسنپ ساخته شد و تاریخ‌ها هنگام اولین بارگذاری/پس از autofill مرورگر خالی می‌شوند.
2. بارگذاری وضعیت دیگر هیچ کلید مفقودی را با اطلاعات نمونه پر نمی‌کند؛ در خطای JSON از چهار نسخه محلی و غنی‌ترین نسخه بازیابی می‌شود.
3. قبل از هر ذخیره rolling backup ساخته و وضعیت با debounce به `/api/state` سرور همگام می‌شود؛ نسخه جدیدتر سرور خودکار بازیابی می‌شود.
4. ترتیب‌ها، customFields، formBoxes، manualLayouts و آرشیو اسنپ در امتیاز بازیابی اولویت بالا دارند.
5. قابلیت‌های حفظ داده/چیدمان و نوار اسنپ به دسترسی‌ها اضافه شد.

# نسخه ۱۱.۲۰.۱

1. بلوک اقلام سفارش ثابت شد: نام = تعداد کالا / تعداد جایزه / جمع مبلغ؛ سپس جمع بدون و با ارزش افزوده، مستقل از تنظیم فیلدهای مدیر.
2. هر دو پیام هم‌نام/جایگذاری زیر نام داروخانه پس از تکمیل خودکار مخفی می‌شوند.
3. دو کلید تهیه گزارش نماینده و افزایش موجودی در بالای اسنپ و با اندازه استاندارد قرار گرفتند.
4. تاریخ‌های سفر/شارژ پیش‌فرض خالی‌اند تا گزارش مستقل سال یا ماه ممکن باشد؛ شارژ خالی/صفر وارد گزارش نمی‌شود.
5. نمایش سه‌رقمی اعداد دیداری در برنامه اضافه شد، با استثنای تاریخ/سال/تلفن/مختصات.
6. قابلیت‌ها در دسترسی‌ها ثبت شدند.

# نسخه ۱۱.۲۰.۰

1. انتخاب «تب مربوطه» در افزودن فیلد مخفی و تب فعال خودکار تعیین می‌شود؛ فیلد هم‌نام، گزینه‌ها و وابستگی‌های فیلد موجود را به ارث می‌برد.
2. گزینه‌های مدیر کشویی دکمه انتخاب دارند و زنجیره استان→شهر→منطقه واقعی اجرا می‌شود؛ CSS تثبیت فرم از حرکت فیلد جلوگیری می‌کند.
3. گزارش سفر اسنپ با ۹ سرستون اعلامی خوانده و تاریخ/ساعت شروع و پایان جدا می‌شود؛ نمایندگان از کاربران غیرمدیر و تطبیق بدون آقا/خانم ساخته می‌شوند.
4. افزایش موجودی فیلتر مستقل سال/ماه/بازه و کلید تهیه گزارش دارد؛ تاریخ/ساعت جدا و بستانکار با عنوان مبلغ شارژ نمایش داده می‌شود.
5. جایگذاری سفارش پس از تکمیل مخفی می‌شود؛ جدول لیست و مودال جزئیات از همان موتور پاک‌سازی/متن ارسال استفاده می‌کنند.
6. ارسال ایمیل پشتیبان به endpoint امن Resend اضافه شد؛ فعال‌شدن نیازمند RESEND_API_KEY و BACKUP_FROM_EMAIL در Render است.
7. همه قابلیت‌ها در دسترسی‌ها ثبت شد.

# نسخه ۱۱.۱۹.۰

1. پیام‌رسان‌ها اکنون همه فیلدهای واقعی صفحه (از جمله نام/شماره مسئول سفارش) را می‌آورند و مدیر با شماره ترتیب، ترتیب ارسال هر فیلد را تعیین می‌کند.
2. قالب اقلام به «نام = تعداد کالا / تعداد جایزه» تغییر کرد؛ جایزه همیشه نمایش داده و تاریخ سفارش برای ارسال به روز/ماه/سال تبدیل شد.
3. آرشیو سفر اسنپ حذف‌ناپذیر شد؛ ورود جدید بالای قبلی می‌نشیند، dedupe حفظ و داده به‌دلیل قرارگیری در state داخل همه پشتیبان‌هاست.
4. فیلد نماینده و کلید تهیه گزارش اضافه، سرستون واقعی فایل استفاده و پنل اسنپ/جستجو/منزل/مرخصی شبکه‌ای و مرتب شد.
5. ورودی جداگانه «گزارش افزایش موجودی» با آرشیو دائمی، جمع سال/ماه/بازه و خروجی اکسل اضافه شد.
6. کنترل‌های رصد تردد (بروزرسانی، همه نمایندگان، پاک‌کردن جستجو، اکسل) بازگردانده و تثبیت شد.
7. تارگت دارای تعداد کالا، ریال واحد پخش/داروخانه، جمع هر دو، جمع تفکیکی نماینده و جمع کل همه نمایندگان شد.

# نسخه ۱۱.۱۸.۰

1. تب مدیرمحور «اسنپ سازمانی» به منوی بالا، سایدبار و بدنه برنامه اضافه شد؛ هیچ نام کاربری/رمزی در پروژه ذخیره نمی‌شود و ورود CAPTCHA عمداً دستی و امن است.
2. بازکردن پنل رسمی، تاریخ پیش‌فرض دیروز، ورود هم‌زمان چند فایل CSV/XLSX، حذف ردیف تکراری دو خروجی، ذخیره گزارش‌ها و نمایش ستون‌های ۱،۲،۵،۹،۱۲،۱۵،۱۸،۱۹،۲۴ اضافه شد.
3. چهار فیلد سال، ماه، از تاریخ و تا تاریخ + جمع کل، تعداد سفر و تجمیع به تفکیک نماینده اضافه شد.
4. خروجی اکسل نمای فیلترشده اضافه و باگ دانلود دوتایی XLS+CSV حذف شد؛ هر کلیک فقط یک فایل می‌گیرد.
5. رنگ سراسری کلیدهای اکسل سبز و حذف قرمز شد.
6. موقعیت زنده دارای گزینه «همه نمایندگان»، fitBounds هم‌زمان و ستون آدرس متنی خودکار برای هر نماینده شد.
7. قانون امنیتی ۶۹: اسرار چت هرگز در کد/chat.arena/ZIP ثبت نشوند و CAPTCHA دور زده نشود.

# نسخه ۱۱.۱۷.۱

1. باگ اصلی «تعداد خالی = ۱» در مسیر فعال ذخیره سفارش حذف شد؛ کالای با تعداد صفر/خالی وارد سفارش، ارسال یا جمع مبلغ نمی‌شود. رکوردهای قدیمی با الگوی مشخص این خطا نیز هنگام ارسال پاک‌سازی می‌شوند.
2. تعیین اطلاعات پیام‌رسان از حالت فهرست ثابت خارج شد و تمام ستون‌های واقعی و سفارشی لیست‌های داروخانه، پزشک و سفارش را پویا می‌خواند؛ تیک برداشته‌شده قطعاً از متن حذف می‌شود.
3. مدیر کشویی‌ها تمام عرض پنل را می‌گیرد و کارت‌ها کنار هم‌اند؛ پنل قدیمی مخفی شد و هر تب فقط شناسه‌های متعلق به خودش را نشان می‌دهد.
4. نام‌های داروخانه/پزشک دیگر یکجا رندر نمی‌شوند؛ جستجوی حداقل دو حرف، حداکثر ۵۰ نتیجه با ویرایش/حذف نشان می‌دهد و برای بیش از ۱۰هزار رکورد آماده است.
5. تاریخ سفارش همیشه فعال است؛ نام داروخانه پس از ثبت یا خروج/بازگشت به تب پاک می‌شود؛ هشدار جایگذاری به‌صورت شناور کنار ورودی است و ترتیب فرم را جابه‌جا نمی‌کند.
6. نشان نسخه فقط برای مدیر/سرپرست نمایش داده می‌شود.
7. ذخیره و اعمال دوباره ترتیب، ترتیب لیست، عرض و ارتفاع پیکسلی اطلاعات کالا در لایه نهایی تضمین شد.

# نسخه ۱۱.۱۷.۰

1. شناسه و جای فیلدها تثبیت شد؛ جابه‌جایی خودکار DOM هنگام ورود نسخه جدید متوقف شد.
2. هشدار داروخانه تکراری سفارش کنار همان فیلد نمایش داده می‌شود و جایگذاری، استان/شهر/منطقه/آدرس و فیلدهای سفارشی متناظر را دقیق پر می‌کند.
3. مدیر کشویی‌ها در ابتدای تب افزودن‌ها، با کارت‌های چندستونه واقعی و عنوان‌های کاملاً فارسی نمایش داده می‌شود؛ سال/ماه از مدیر کشویی حذف‌اند.
4. مدیریت نام داروخانه و پزشک همراه زیرمجموعه‌ها، ویرایش ارجاع‌ها و حذف تأییدشده اطلاعات وابسته اضافه شد.
5. همه لیست‌های اصلی تازه‌ترین رکورد را بالا نشان می‌دهند. مدیر تعیین می‌کند چه فیلدهایی در پیام‌رسان‌ها ارسال شود و کاربر حق تغییر ندارد.
6. GPS با شروع ویزیت فعال می‌شود و کارت‌های مسافت متر، توقف، نقاط و ساعت شروع را نشان می‌دهد. پایان ویزیت، سابقه دائمی تفکیک‌شده را در رصد تردد ثبت می‌کند.
7. رصد تردد دارای جستجوی لحظه‌ای و خروجی اکسل است؛ قانون ردیف/نام نماینده برای همه خروجی‌ها تثبیت شد.
8. آدرس معکوس با جزئیات کوچه/راه/پلاک و نمایش کامل Nominatim ذخیره می‌شود.
9. نسخه دقیق ۱۱.۱۷.۰ در هدر دیده می‌شود. workflow گیت‌هاب از دستور اشتباه Next.js پاک شد تا ضربدر قرمز کاذب برطرف شود.

# نسخه ۱۱.۱۶.۲

1. اصلاحات مدیر کشویی‌های افزودن‌ها (به گزارش کاربر): کارت‌های فیلدها حالا کنار هم (شبکه‌ای) چیده می‌شوند نه همه زیر هم؛ نام‌های انگلیسی با واژه‌نامه فارسی + خواندن placeholder رفع شد؛ فیلدهای «سال» و «ماه» تاریخ شمسی از این بخش حذف شدند و «نوع داروخانه» مثل بقیه کشویی‌ها یک کارت عادی است.
2. رفع ناهماهنگی طوسی‌ها: موتور طوسی حالا روی کل بلوک فیلد (شامل ورودی دیداری کمبوها) اعمال می‌شود — دیگر «نصف طوسی نصف سفید» نیست؛ و با حفظ موقعیت اسکرول، پدیده «کشیده شدن فیلد به بالا بعد از انتخاب استان» برطرف شد.
3. سفارشات: فیلدهای کشویی طوسی واقعاً غیرقابل‌انتخاب می‌شوند (قفل روی خود کمبو)، و برخلاف قبل، بخش کالا (تعداد کالا/جایزه/کاتالوگ) همیشه فعال و عادی می‌ماند. اگر جای فیلدها در تب داروخانه عوض شود، همان ترتیب در سفارشات هم اعمال می‌شود.
4. دکمه «تغییر رمز» دیگر روی دکمه خروج نیفتاده؛ داخل هدر درست کنار خروج می‌نشیند.
5. رفع هشدار اشتباه «قبلاً ثبت شده» برای رکورد جدید: امضای رکوردی که لحظه‌ای پیش خودکار ذخیره شده به خاطر سپرده می‌شود و ثبت دوباره‌اش با پیام دوستانه (بدون ایجاد رکورد تکراری) جلوگیری می‌شود؛ رکوردِ در حال ویرایش هم از بررسی تکراری مستثنا است.
6. قانون تکیدی کاربر: ابتدا PROJECT_GRAPH.md خوانده شود، نه کل کدها (همان قانون ۶۶ — تأیید مجدد).

# نسخه ۱۱.۱۶.۱

1. رفع مشکل آپدیت نشدن گیت‌هاب: مشخص شد روی گیت‌هاب یک کامیت دستی «پروژه اولیه» با محتوای 11.15.3 جایگزین تاریخچه شده و به‌خاطر نخوردن تاریخچه‌ها push رد می‌شد. اسکریپت PUSH_FRESH_GITHUB.bat ساخته شد: ثبت فایل‌ها ← ادغام امن با allow-unrelated-histories (فایل‌های شما برنده) ← push. بعد از این یک‌بار، SYNC_ALL.bat دوباره مثل قبل کار می‌کند.
2. گیت‌لب بدون توکن: روش کلید SSH به RAHNAMA_GITLAB.txt اضافه شد (ssh-keygen ← افزودن کلید در SSH Keys ← تغییر ریموت به git@gitlab.com). تا وقتی گیت‌لب وصل نشود، SYNC_ALL.bat خودش تشخیص می‌دهد و فقط گیت‌هاب را به‌روز می‌کند؛ پشتیبانی گیت‌لب در فایل‌ها حفظ است.
3. تغییری در صفحات برنامه نسبت به ۱۱.۱۶.۰ نیست (نسخه ترمیمی تحویل/ابزار).

# نسخه ۱۱.۱۶.۰

1. گراف دانش پروژه: فایل PROJECT_GRAPH.md ساخته شد که همه فایل‌ها، توابع، روابط window بین لایه‌ها، نقشه API، نقشه حافظه و نقشه تب‌ها را یکجا دارد تا هوش مصنوعی به‌جای خواندن کل سورس فقط همین را بخواند (صرفه‌جویی توکن). سازنده خودکار: update_project_graph.py — در پایان هر تحویل بازسازی می‌شود.
2. تب افزودن‌ها: هر فیلد کشویی هر تب با نام فارسی نمایش داده می‌شود و همه زیرمجموعه‌هایش دقیقاً زیر همان فیلد (زیر هم) آمده‌اند؛ کنار هر زیرمجموعه دکمه ✏️ ویرایش و 🗑️ حذف است، هر فیلد جستجوی لحظه‌ای و کادر افزودن گزینه دارد و با افزودن/حذف فیلد کشویی جدید، همین بخش خودکار به‌روز می‌شود.
3. زنجیره طوسی: فیلدهای وابسته (مثل شهر/منطقه) تا انتخاب والد (مثل استان) طوسی و غیرفعال‌اند و بعد از انتخاب والد عادی می‌شوند. مدیر برای هر فیلد می‌تواند والدش را مشخص یا حالت را خاموش کند و یک کلید کلی هم در افزودن‌ها هست.
4. تب سفارشات: تا وقتی داروخانه انتخاب و «جایگذاری خودکار» نشده، همه فیلدها جز نام داروخانه طوسی‌اند؛ بعد از جایگذاری، اطلاعات داروخانه سر جایشان می‌نشیند (طوسی می‌مانند) و فقط بخش کالا (تعداد کالا/جایزه) فعال و عادی می‌شود. داروخانه‌های ثبت‌شده همه کاربران یکجا در سفارشات دیده می‌شود.
5. تکراری‌گیر دو مرحله‌ای: اگر اطلاعات داروخانه/پزشک دقیقاً مثل رکورد موجود باشد، ثبت کاملاً مسدود می‌شود و پیام می‌دهد «فقط ویرایش از لیست مجاز است» (چه خودت ثبت کرده باشی چه کاربر دیگر). اگر فقط نام یا تلفن نزدیک بود، همان پنجره قبلی تأیید/انصراف می‌آید.
6. فلش‌های بالا/پایین ورودی‌های عددی تعداد کالا و تعداد جایزه در تب سفارشات برداشته شد.
7. دکمه «🔑 تغییر رمز» بالای همه صفحه‌ها: هر کاربر رمز خودش را عوض می‌کند و همان لحظه در سیستم ورود (CRM_USERS_AUTH + state.users) اعمال می‌شود.
8. سطوح دسترسی آماده در تب کاربران و دسترسی: نماینده علمی، کارشناس فروش، سرپرست — با یک انتخاب، همه تیک‌های دسترسی کاربر یکجا تنظیم می‌شود؛ برای کاربر جدید یا موجود، بدون تعریف دوباره.
9. عیب‌یابی سالم شد: خطای «شی state در دسترس نیست» رفع شد (state حالا در window در دسترس است)، ردیف /api/state دیگر هشدار بی‌ربط نمی‌دهد («ذخیره سروری غیرفعال — عادی») و متن قدیمی بالای صفحه جایگزین شد.
10. در تب داروخانه اگر نام داروخانه جدید را با «➕ افزودن» لحظه‌ای اضافه کنی، رکورد خودش خودکار ذخیره می‌شود؛ نیازی به زدن جداگانه دکمه ذخیره نیست.
11. رفع نقص ورود فیلد جدید به بخش کالا (ستون‌ها و کالاها): فیلدها حالا با اندازه و شماره ترتیب واقعی وارد فرم کالا می‌شوند (در انتظار تأیید مرورگری). اعمال «شماره ترتیب لیست» روی جدول‌های لیست تقویت شد: ردیف همیشه ستون اول و نام نماینده ستون دوم است.
12. هر فیلدی که مدیر در تب داروخانه اضافه/حذف کند، خودکار در تب سفارشات هم اضافه/حذف می‌شود (همگام‌سازی آینه‌ای).

# نسخه ۱۱.۱۵.۳

1. قانون جدید کاربر برای همگام‌سازی فایل‌ها: اسکریپت «SYNC_ALL.bat» (ویندوز) و «sync_all.sh» (لینوکس/مک) ساخته شد؛ با یک دستور، تغییرات محلی اول از گیت‌هاب و گیت‌لب pull و بعد به هر دو push می‌شود. راهنمای یک‌بار تنظیم گیت‌لب در RAHNAMA_GITLAB.txt آمده است.
2. فایل‌ها مستقل از سیستم عامل شدند: «.gitattributes» اضافه شد تا خط‌پایان‌ها بین ویندوز و لینوکس/مک به‌هم نریزد (bat/ps1 با CRLF و sh/py/mjs با LF)؛ چون کاربر با دو سیستم کار می‌کند و فولدر برنامه را مدام بین دو سیستم جابه‌جا می‌کند.
3. امنیت .env: بررسی کل تاریخچه گیت نشان داد هیچ فایل .env واقعی تاکنون وارد گیت‌هاب نشده است. .gitignore محکم‌تر شد (هر .env و .env.* به‌جز قالب‌های example؛ node_modules در هر سطح؛ server-db.json؛ *.zip) و رمز نمونه admin1234 از فایل قالب .env.ndcohub.example حذف شد.
4. node_modules برای همیشه غیرضروری شد: بررسی نشان داد server.js و همه اسکریپت‌های scripts فقط از ماژول‌های داخلی Node استفاده می‌کنند؛ وابستگی‌های بلااستفاده express و cors از package.json حذف شد و package-lock.json سبک بازسازی شد. دیگر npm install لازم نیست؛ حذف امن روی ویندوز: rmdir /s /q node_modules و روی لینوکس/مک: rm -rf node_modules.
5. مرحله build فایل .gitlab-ci.yml از مراحل خفته اسکلت Next (typegen/tsc) پاک شد تا CI گیت‌لب سبز بماند.
6. لیست سفید پاکسازی: KEEP_ONLY_GITHUB.bat که جا افتاده بود اضافه شد و فایل‌های جدید این نسخه هم به OFFICIAL_FILELIST.txt افزوده شدند (۲۳۹ ورودی). در این نسخه هیچ تغییری در صفحات و فرم‌های برنامه نداریم.

# نسخه ۱۱.۱۵.۲

1. هشدار «داروخانه/پزشک تکراری» دیگر فقط یک دکمه OK ندارد؛ به پنجره انتخاب دودکمه‌ای تبدیل شد: تأیید/بله = با این حال ذخیره شود، انصراف/خیر = ذخیره نشود. رفتار قبلی (ذخیره خودکار بعد از OK) حذف شد.
2. این تبدیل در هر ۴ مسیر ثبت اعمال شد: داروخانه و پزشک در crm-features-v9.js (مسیر فعال) و دو نسخه تکراری در crm-app.js.

# نسخه ۱۱.۱۵.۱

1. ریشه اصلی اخطار اجباری پیدا و حذف شد: ۹ بررسی سخت‌کدشده (بدون ربط به سیستم ستاره) در ذخیره داروخانه، پزشک و سفارش — سه‌تایی در crm-features-v9.js و شش‌تایی در دو نسل کد crm-app.js — حذف شدند. پیام‌هایی مثل «لطفاً نام، استان، شهر، منطقه و آدرس/لوکیشن داروخانه را کامل کنید.» دیگر بدون توجه به ستاره اجرا نمی‌شوند.
2. از این نسخه به بعد در کل برنامه و همه تب‌ها فقط فیلدهایی که مدیر ستاره‌دار کرده موقع ذخیره اجبار می‌کنند؛ همه مسیرهای ذخیره به اعتبارسنج واحد ستاره‌محور (validateRequiredFields) وصل شدند که فیلدهای مخفی یا حذف‌شده را هم نادیده می‌گیرد.
3. بررسی «حداقل یک قلم کالا در سفارش» به‌عنوان قانون کسب‌وکار حفظ شد (ربطی به ستاره فیلدها ندارد).

# نسخه ۱۱.۱۵.۰

1. اجبار ستاره‌دار قدیمی پاک شد؛ فقط «نام داروخانه» و «نام پزشک» به‌صورت پیش‌فرض الزامی‌اند و ثبت دیگر به‌خاطر فیلدهای خالی دیگر اخطار نمی‌دهد (مهاجرت یک‌باره روی داده فعلی اعمال می‌شود).
2. در تب افزودن‌ها کنترل‌های هر تب کنار هم چیده شد؛ جعبه «فهرست فشرده همه کشویی‌ها» و جعبه «اطلاعات فیلدها» از این تب برداشته شد؛ دکمه رنگی کوچک «افزودن گزینه جدید» حذف شد و حالا با تایپ داخل کشویی، گزینه «➕ افزودن "متن"» همان لحظه به زیرمجموعه همان کشویی اضافه می‌شود. وابستگی استان/شهر/منطقه حفظ شده است.
3. همه دکمه‌های حذف برنامه آیکن 🗑️ و همه دکمه‌های ویرایش آیکن ✏️ شدند.
4. تیک «افزودن لحظه‌ای گزینه» برای همه انواع فیلد در افزودن فیلد و ویرایش فیلد فعال است و برای فیلدهای ثابت فرم هم ذخیره می‌شود.
5. ستون اول لیست‌ها ردیف و ستون دوم نام نماینده است؛ «شماره ترتیب در لیست» حالا واقعاً ترتیب ستون‌های جدول لیست را عوض می‌کند (جدا از ترتیب فرم).
6. ستون مسیریابی در لیست‌ها به‌جای متن، آیکن نقشه/GPS در هر سطر دارد.
7. دکمه «پاک کردن» به فرم‌های داروخانه‌ها، پزشکان و سفارشات اضافه شد.
8. حذف فیلد در جدول اطلاعات فیلدها فیلد را کاملاً حذف می‌کند (نه فقط مخفی)؛ برداشتن تیک «نمایش در فرم» دیگر دکمه حذف را به «نمایش مجدد» تبدیل نمی‌کند.
9. کنار دکمه حذف هر قلم سفارش، دکمه ویرایش ✏️ اضافه شد که کالای همان سطر را در تب ستون‌ها و کالاها برای ویرایش باز می‌کند.
10. «اطلاعات کادرها» و «اطلاعات کلیدها» فقط در بخش خودشان نمایش داده می‌شوند و از جدول اطلاعات فیلدها برداشته شدند.
11. کالاها: دکمه «+کالای جدید» حذف شد؛ ورودی‌های «افزودن فیلد به فرم کالا» لیبل گرفتند؛ «شماره ترتیب در کادر کالا» و «شماره ترتیب در لیست» جدا هستند و واقعاً کار می‌کنند؛ فیلدهای تازه درست زیر همان کادر در «اطلاعات کالا» می‌آیند (ویرایش عرض/ارتفاع/ترتیب + جدول کالاها با ویرایش/حذف کارا)؛ دکمه «ویرایش جدول» حذف شد؛ «تعداد» به «تعداد کالا» تغییر نام داد؛ تصویر جایزه برداشته شد؛ هر سطر اقلام «جمع مبلغ» فقط‌خواندنی دارد (تعداد کالا × قیمت واحد داروخانه)؛ درصد ارزش افزوده نمایش داده می‌شود و فقط مدیر می‌تواند تغییرش دهد؛ دو جمع کل فقط‌خواندنی (بدون ارزش افزوده / با ارزش افزوده) زیر اقلام است؛ فرمول جمع (کدام فیلد × ÷ + − کدام فیلد) در اطلاعات کالا برای مدیر قابل تنظیم است؛ فلش کشویی نوع فیلد دیگر لبه صفحه نمی‌افتد؛ صفر پیش‌فرض با اولین تایپ پاک می‌شود؛ همه اعداد برنامه جداکننده سه‌رقمی دارند؛ جمع کل سفارش اصلاح شد؛ با افزودن کالا به سفارش، تعداد کالا و تعداد جایزه برای تنظیم مدیر نمایش داده می‌شود.
12. صفحه پشتیبان‌گیری از چیدمان خودکار مستثنا شد تا به‌هم نریزد؛ پشتیبان خودکار واقعاً اجرا می‌شود؛ پشتیبان دستی در پوشه‌ای که مدیر انتخاب می‌کند ذخیره می‌شود (در Chrome/Edge با انتخاب پوشه و دسترسی ذخیره‌شده برای دفعات بعد؛ در سایر مرورگرها با پنجره ذخیره در محل دلخواه)، نه بی‌صدا در Downloads.
13. عیب‌یابی ریز شد: هر بخش جدا گزارش می‌شود — وضعیت آنلاین، حجم localStorage، نشست کاربر، شمارش داده‌ها، فیلدهای سفارشی/حذف‌شده، آخرین پشتیبان، سرویس‌ورکر، کش‌ها، IndexedDB، File System Access، Leaflet، سلامت سرور با زمان پاسخ و نسخه — و جمع‌بندی مشکل‌ها در کادر وضعیت می‌آید؛ دکمه «بررسی مجدد اتصال» حالا همین عیب‌یابی واقعی را اجرا می‌کند نه تست ساختگی.
14. قانون دائمی ثبت شد: هر امکانِ اضافه/حذف/تغییرشده باید در «دسترسی‌ها» هم منعکس شود. گروه «ابزارهای مدیریت (نسخه ۱۱.۱۵)» با کلیدهای جدید (طراحی دستی، کپی تب‌ها، افزودن‌ها، ویرایش جغرافیا، پشتیبان، بازیابی، عیب‌یابی، نصب، تارگت فروش، فرمول سفارش) به صفحه دسترسی‌ها اضافه شد.

# نسخه ۱۱.۱۴.۰

1. ستاره سیاه لیبل حذف شد؛ ستاره قرمز بدون فاصله دقیقاً کنار متن لیبل می‌نشیند. ثبت با فیلد خالی ستاره‌دار اخطار فارسی می‌دهد: «فیلد «X» خالی است و باید پر شود.» اخطار انگلیسی مرورگر با novalidate قطع شد.
2. در اضافه کردن کالا: شماره ترتیب، زیرهم/روبرو، عرض و ارتفاع پیکسل به نوار افزودن فیلد آمد.
3. ستون «سایز لیبل/فیلد» شد «سایز فونت لیبل/فیلد» با محدوده ۱۰–۲۸ تا عدد بزرگ فونت را غول نکند و فیلدهای بعدی از صفحه نپرند.
4. ترتیب فرم و ترتیب لیست جدا شد؛ هر دو ستون در اطلاعات فیلدها هست و همه فیلدهای همان تب دیده می‌شوند.
5. کشویی «چه چیزی اضافه شود؟» فیلد / کادر / کلید. جدول اطلاعات کادرها و اطلاعات کلیدها با ویرایش/حذف مثل اطلاعات فیلدها اضافه شد.
6. کنار عرض، ارتفاع هست. عرض و ارتفاع عدد واقعی همان فیلد/کادر/کلید را نشان می‌دهد نه همیشه ۲۲۰.
7. کلید حذف ساخته‌شده کنار سطر همان ردیف را حذف می‌کند. ثبت/ویرایش/حذف پیغام تأیید دارند. حذف قرمز، ذخیره سبز تیل، ویرایش outline.
8. روی بوم خط‌دار طراحی دستی کادر، کلید، فیلد و کارت قابل انتخاب/درگ است (تاریخ تقویم کلیک را قفل نمی‌کند). کپی تب: کل امکانات یا انتخاب بخشی با تیک ریز.

# نسخه ۱۱.۱۳.۰

1. گزینه «زیر هم» دیگر با عرض فیلد خنثی نمی‌شود؛ فیلد یک سطر جدا می‌گیرد و عرض فقط خود اینپوت را بزرگ می‌کند.
2. عرض فیلد در فرم افزودن/ویرایش است؛ ستون عرض از جدول اطلاعات فیلدها برداشته شد.
3. بعد از ذخیره ویرایش، کادر اطلاعات فیلدها همان‌جا (با همان اسکرول) برمی‌گردد.
4. حذف یک فیلد دیگر فیلدهای بعدی همان کادر را پنهان نمی‌کند.
5. شناسه‌های لاتین فیلد/کلید با نام فارسی نشان داده می‌شوند.
6. تیک ستاره‌دار ستاره قرمز کنار لیبل می‌گذارد و پر کردن را الزامی می‌کند.
7. داروخانه ثبت‌شده در تب داروخانه‌ها در سفارشات مثل پیش‌فرض‌ها اخطار «قبلاً ثبت شده» و پر کردن خودکار می‌دهد.
8. فیلدهای روبرو روی یک خط تراز شدند (تاریخ ثبت دیگر پایین‌تر نیست).
9. طراحی دستی: بوم خط‌دار، کادر فرم ثبت هدف درگ نیست، فیلدها قفل و غیرقابل تایپ‌اند، اندازه از گوشه عوض می‌شود.
10. تیک «خروجی اکسل» در افزودن فیلد و جدول اطلاعات؛ ستون تیک‌خورده در خروجی اکسل می‌آید.

# نسخه ۱۱.۱۲.۰

1. کشویی‌ها (استان و بقیه) با تایپ لحظه‌ای فیلتر می‌شوند؛ حرف اول همان‌هایی را می‌آورد که با آن شروع می‌شوند.
2. تب افزودن فشرده‌تر شد. برای شهر اول استان انتخاب می‌شود بعد شهرهای همان استان؛ منطقه هم بعد از شهر.
3. نام داروخانه‌ای که در تب داروخانه اضافه شود در کل برنامه (سفارشات، جستجو، تکراری) همان رکورد است.
4. فریز سرستون و نوار اسکرول افقی زیر سرستون بعد از ساخته‌شدن جدول طراح دوباره اعمال می‌شود. سایز عددی عرض خود فیلد را عوض می‌کند.
5. در طراحی دستی روی فیلد/کلید یک لایه قفل است؛ کلیک انتخاب می‌کند نه پر کردن فرم.

# نسخه ۱۱.۱۱.۰

1. در ستون‌ها اندازه فیلد دیگر برای همه ۲۲۰ نیست؛ آدرس بزرگ‌تر دیده می‌شود و تغییر اندازه روی خود فیلد ذخیره و اعمال می‌شود.
2. سرستون جدول فریز است و نوار اسکرول افقی همان کادر، زیر سرستون می‌ماند نه ته جدول.
3. دکمه افزودن فیلد ساده دوباره به `customFields` می‌نویسد (کلید type=button + ذخیره مستقیم).
4. تنظیمات قبلی کاربر پاک نمی‌شود؛ قبل از این نسخه یک پشتیبان محلی گرفته می‌شود. فیلد داروخانه سفارش دیگر تمام‌عرض اجباری نیست.
5. طراحی دستی: فیلدها حالت پر کردن فرم ندارند؛ کلیک = انتخاب برای اسم و اندازه. تغییر اندازه فرم ثبت، لیست را حذف نمی‌کند.

# نسخه ۱۱.۱۰.۰

1. تب افزودن‌ها مثل ستون‌ها گرید همه تب‌ها را دارد؛ کشویی همان تب با زیرمجموعه و ویرایش/حذف روبه‌رو.
2. در اضافه کردن کالا می‌شود فیلد ساخت و کالا را ویرایش کرد.
3. ذخیره فیلد ساده: مقدار فیلد سفارشی دیگر هنگام ثبت داروخانه/پزشک/سفارش/کالا پاک نمی‌شود.
4. در سفارش، با تایپ نام داروخانه همه داروخانه‌های هم‌نام با استان/شهر/آدرس می‌آید تا یکی انتخاب شود.
5. کنار حذف تب، ویرایش نام و شماره تب اضافه شد.
6. کپی‌از / کپی‌به در طراحی دستی دیگر زیر بوم خط‌کشی نمی‌رود.

# نسخه ۱۱.۹.۱

1. روز هفته تقویم شمسی با تاریخ واقعی تهران جور است (۱ مرداد ۱۴۰۵ = پنجشنبه؛ امروز ۲۲ مرداد = پنجشنبه).
2. کادر کوچک میلادی فقط JAN…DEC و روز تهران را نشان می‌دهد (مثلاً AUG ۱۳)، نه نام ماه محلی.
3. فیلد تاریخ ساخته‌شده توسط مدیر همان تقویم کنار فیلد تاریخ داروخانه را دارد.
4. در فیلدهای ساده، با تایپ اسم دکمه «افزودن «متن»» کنار همان فیلد ظاهر می‌شود.

# نسخه ۱۱.۹.۰

1. امکانات آماده روی تب جدید واقعاً یک فیلد/کلید می‌سازد (نه سکوت، نه ده تا).
2. تب تازه‌ساخته خالی است؛ مربع‌های ثبت/بازنشانی و کادر لیست پیش‌فرض حذف شد.
3. طراحی دستی: همان امکانات آماده + ساخت کادر؛ همه فیلد و کلید و نقشه قابل درگ است.
4. برچسب رنگی می‌گوید کلید برای ردیف است، کادر است یا کل فرم.

# نسخه ۱۱.۸.۰

1. امکانات آماده روی تب جدید دیگر با هر کلیک ۱۰ بار ساخته نمی‌شود؛ هر کلید یک‌بار می‌آید.
2. تب «طراحی دستی تب‌ها»: همه تب‌ها مثل ستون‌ها اینجاست؛ با کلیک همان صفحه می‌آید.
3. درگ چپ/راست/بالا/پایین و تغییر اندازه فیلد، کلید، کادر و نقشه.
4. کپی امکانات و چیدمان از یک تب به تب دیگر.
5. شکل نقشه ایران عوض نشده است.

# پاکسازی درخت فایل (همراه ۱۱.۷.۰)

روی گیت‌هاب این فایل‌های اضافه بود: زباله WinRAR، crm-enhancements.js،
iran-provinces.geojson، icon/logo.svg و src-svg.
از این محیط پوش به گیت‌هاب ممکن نیست. روی ویندوز:
CLEAN_EXTRA_FILES.bat و بعد PUSH_GITHUB_CLEAN.bat را اجرا کن.

# نسخه ۱۱.۷.۰

1. تب جدید مدیر به منوی اصلی و سایدبار هم اضافه می‌شود (قبلاً فقط داخل ستون‌ها دیده می‌شد).
2. مدیر شماره ترتیب همه تب‌ها (قدیمی و جدید) را مشخص می‌کند.
3. ساخت کادر: نام، تب مقصد، و انتخاب فیلد/کلید برای گذاشتن داخل کادر.
4. امکانات آماده شامل فیلد متنی/عددی/تاریخ/کشویی/تلفن/متن بلند + نقشه و کلیدها.
5. جای فیلد (روبرو/زیر هم) و داخل کدام کادر هنگام ثبت فیلد مشخص می‌شود.
6. کلید ویرایش و حذف لیست‌ها مخفی نمی‌شود.
7. لوگوی TENIN TEB TAHA طبق تصویر کاربر بزرگ و واضح در هدر، سایدبار و ورود.

# نسخه ۱۱.۶.۰

1. فیلد اضافه‌شده دیگر پاک و دوباره ساخته نمی‌شود؛ روی گرید اصلی همان تب می‌نشیند.
2. برای هر کادر دکمه ویرایش هست (ثبت کادر از قبل بود).
3. کلیدها و امکانات اصلی (موقعیت فعلی، دریافت آدرس، نقشه، ثبت، حذف، ویرایش، فایل…) در طراح ستون‌ها می‌آیند تا مدیر داخل کادر بگذارد.
4. مدیر می‌تواند تب جدید بسازد؛ تب به منو اضافه می‌شود، آیکون از روی اسم می‌آید، فونت و اندازه مثل تب‌های فعلی است و با همان امکانات طراحی می‌شود.

# نسخه ۱۱.۵.۱

1. تب‌های اصلی برگشت: خطای نحوی انتهای crm-app.js منو را می‌خواباند و فقط داشبورد دیده می‌شد.
2. منوی بالا و سایدبار دیگر در HTML هم هستند؛ اگر جاوااسکریپت خطا بدهد تب‌ها حذف نمی‌شوند.
3. تب‌های داخل «ستون‌ها و کالاها» که با CSS مخفی شده بودند دوباره دیده می‌شوند و هم‌اندازه تب داشبورد هستند.

# نسخه ۱۱.۵.۰

1. فونت لیبل و فونت خود فیلد جدا + سایز عددی.
2. تب‌های ستون‌ها هم‌اندازه تب داشبورد.
3. سرستون جداول هنگام اسکرول فریز است.
4. ستاره الزامی برای هر فیلد + اخطار اگر خالی بماند.
5. دکمه‌های هم‌نام یک شکل شدند.
6. کادر اقلام سفارش دیگر تکرار نمی‌شود؛ می‌توان کادر گروهی ساخت و فیلدها را داخلش گذاشت.

# نسخه ۱۱.۴.۱

1. تکرار فیلد اقلام سفارش در طراح ستون‌ها حذف شد (فقط ۴ ستون کالا/تعداد/جایزه/قیمت یک‌بار می‌آید).
2. دکمه ویرایش کنار هر کالا در اضافه کردن کالا.
3. چینش روبرو/زیر هم واقعاً روی فرم اعمال می‌شود.
4. برای هر فیلد: فونت، بولد، اندازه نوشته.

# نسخه ۱۱.۴.۰

1. فیلد جدید در تب ستون‌ها واقعاً روی فرم تب اصلی همان‌جا می‌نشیند.
2. شماره ردیف = جای واقعی فیلد در فرم (۵ یعنی پنجمین فیلد).
3. چینش «روبرو» یا «زیر هم» برای هر فیلد.
4. تیک نمایش در فرم روی خود فرم اعمال می‌شود (نه فقط لیست).
5. همه فیلدهای صفحه (از جمله عرض/طول و سطر اقلام سفارش) در لیست طراح می‌آیند.
6. همه کشویی‌های سیستم در تب افزودن‌ها لیست می‌شوند و می‌شود گزینه اضافه کرد.

# نسخه ۱۱.۳.۰

1. ستون ترتیب مثل شماره ردیف است: عدد ۴ یعنی همان فیلد می‌رود ردیف ۴ فرم و جدول.
2. دکمه‌های ▲▼ فیلد را یکی‌یکی بالا/پایین می‌برند و شماره‌ها از نو ۱،۲،۳ می‌شوند.
3. اندازه (پیکسل) عرض واقعی فیلد در فرم را عوض می‌کند.
4. تیک «نمایش در فرم» و «نمایش در لیست» برای همه فیلدهای موجود (ثابت و اضافه‌شده) اضافه شد.

# نسخه ۱۱.۲.۰

1. در ستون‌ها و کالاها، همه فیلدهای فرم همان تب دیده می‌شود (ثابت بالای کادر درصد + سفارشی).
2. روبه‌روی هر فیلد دکمه ویرایش هست.
3. حذف فیلد، همان فیلد را از فرم تب اصلی هم برمی‌دارد (نه فقط از لیست طراح).
4. هر کالای ثبت‌شده در تب سفارشات با فیلد تعداد جایزه ظاهر می‌شود.

# نسخه ۱۱.۱.۰

1. تب «ستون‌ها و کالاها»: همه تب‌های برنامه مثل داشبورد به‌صورت کارت داخل همین صفحه می‌آید.
2. با کلیک روی هر تب، فقط حذف و اضافهٔ فیلد همان تب باز می‌شود تا صفحه شلوغ نشود.
3. شماره ترتیب واقعاً جای فیلد را در فرم همان تب عوض می‌کند (۱ = اولین فیلد). دکمه‌های بالاتر/پایین‌تر هم همان کار را می‌کنند.
4. عنوان بخش کالا: «اضافه کردن کالا» (به‌جای کاتالوگ دارویی).
5. فیلد «دسته درمانی» حذف شد.
6. بعد از ثبت کالا، همان نام کالا در بنر سبز، داخل فیلد نام، و در ردیف جدول نمایان می‌ماند.

# نسخه ۱۱.۰.۰

1. تغییر رمز هر کاربر و اعمال در ورود بعدی (localStorage + state)
2. نشانگر میلادی امروز ایران
3. افزودن لحظه‌ای برای فیلدهای تایپی
4. شماره ردیف واقعی ۱،۲،۳ در لیست‌ها
5. خروجی اکسل با جدول خط‌کشی‌شده (.xls)
6. فعالیت لحظه‌ای: نمودار + نقشه
7. مختصات با دقت بیشتر و GPS دقیق
8. نقشه جامع ایران / منطقه / جستجو / مراکز شاخص
9. موقعیت زنده به تفکیک نماینده
10. تب شروع/پایان ویزیت با ادامه مسیر در نقطه کور
11. تارگت فروش با سال/ماه/کالا و جمع
12–13. طراحی فیلد برای همه تب‌ها + افزودن‌ها
14. تعداد جایزه کنار کالا در سفارش و لیست
15. مخفی‌سازی تب/فیلد بدون دسترسی
16. ریز عملیات در عیب‌یابی

# نسخه ۱۱.۲۴.۰

1. سبک‌سازی بدون حذف اطلاعات یا امکانات: قالب‌بندی عدد فقط روی بخش تازه‌تغییرکرده اجرا می‌شود و رندرهای تکراری شروع برنامه و اتصال تنظیمات کالا به همه ذخیره‌های GPS حذف شد.
2. ثبت‌های کاربر در همان لحظه، فقط در نمای فعال، بازخوانی می‌شوند و نیاز به رفرش دستی ندارند.
3. سطر/نوار فاصله بین سرستون و اولین ردیف همه جدول‌ها حذف شد.
4. قانون سراسری سرستون برنامه و Excel: آبی آسمانی `#87CEEB` با فونت مشکی.
5. GPS با `watchPosition`، دقت بالا، بهترین نقطه تا ۳۰ ثانیه و آستانه ثابت ۱۰ متر کار می‌کند؛ اجزای ریزتر آدرس نیز حفظ می‌شوند.
6. در Excel فقط سلول‌های ساخته‌شده و غیرخالی خط‌کشی دارند؛ Style پیش‌فرض سلول‌های خالی بدون Border است.

# نسخه ۱۱.۲۴.۱

1. کدهای دیتابیس شفاآراد مطابق جدول اصلاحی کاربر ثبت شدند: `1001→1391911001` و `1007→1391902006`؛ پنج نگاشت دیگر نیز دقیقاً تثبیت شدند.
2. نگاشت‌های قدیمی ذخیره‌شده کالاها هنگام شروع برنامه به نگاشت صحیح جدید مهاجرت می‌کنند.
3. ردیف «نامشخص» از گزارش دایا دارو حذف شد و ردیف‌های دایا/شفاآراد فقط وقتی به کالای معتبر برنامه تطبیق شوند وارد گزارش می‌شوند.
4. نشان نسخه بالای صفحه دیگر عدد ثابت ندارد و نسخه را مستقیماً از آدرس فایل فعال برنامه می‌خواند؛ نسخه تحویلی `11.24.1` است.

# نسخه ۱۱.۲۵.۰

1. تب جدید «وضعیت فاکتور پخش‌ها» به منوی بالای برنامه و منوی موبایل اضافه شد.
2. فیلترهای نماینده علمی، سال، ماه، از تاریخ، تا تاریخ و جستجوی لحظه‌ای داروخانه در یک سطر قرار گرفتند؛ سال/ماه و بازه تاریخ از قانون چک‌باکس انحصاری و حالت طوسی استفاده می‌کنند.
3. سفارش‌های برنامه با فاکتورهای چهار پخش فقط در فاصله ۳ روز قبل تا ۳ روز بعد تطبیق داده می‌شوند.
4. تطبیق داروخانه بر پایه نام مشابه/بخشی (مثل عقبایی و عقبایی نژاد) به‌همراه استان، شهر و منطقه انجام می‌شود.
5. جدول شامل نماینده، داروخانه، استان، شهر، منطقه، پخش، تاریخ سفارش، تاریخ فاکتور، اختلاف روز، مشاهده جزئیات و نام ویزیتور است.
6. پنجره جزئیات، تعداد و جایزه سفارش را کنار تعداد و جایزه فاکتور و اختلاف علامت‌دار هرکدام نشان می‌دهد.
7. نتیجه پایه تطبیق Cache می‌شود تا جستجوی لحظه‌ای، فایل‌های حجیم پخش را با هر حرف دوباره اسکن نکند.
8. شش دسترسی مستقل برای تب، فیلترها، جستجو، تطبیق، بازه سه‌روزه و جزئیات اضافه شد.

# نسخه ۱۱.۲۶.۰

1. منبع یکپارچه گزینه‌های کشویی بر پایه نام/معنای لیبل ایجاد شد؛ داروخانه، نماینده علمی، سال، ماه، استان، شهر، منطقه و هر لیبل هم‌نام در کل برنامه یک مجموعه گزینه دارند.
2. افزودن، ویرایش یا حذف گزینه در تب افزودن‌ها یا داخل کشویی، هم‌زمان روی همه فیلدهای هم‌نام و custom fieldهای همان لیبل اعمال و ذخیره می‌شود.
3. فیلد کشویی هم‌نامی که بعداً به هر تب اضافه شود، گزینه‌های منبع جهانی را می‌گیرد و خالی ساخته نمی‌شود.
4. قفل ترتیب از چهار فرم به تمام `.form-grid`های همه تب‌ها گسترش یافت؛ ترتیب هر شبکه جداگانه ذخیره و بعد از رندرهای قدیمی بازیابی می‌شود.
5. مسیر افزودن/حذف آینه‌ای فیلد داروخانه در سفارش دیگر `applyFullFormLayout` مخرب را اجرا نمی‌کند.
6. ناپدیدشدن تب وضعیت فاکتور رفع شد: نمایش/پنهان‌سازی برگشت‌پذیر است و کاربر قدیمی در نبود مجوز جدید، دسترسی فروش پخش قبلی را به ارث می‌برد.
7. سرویس‌ورکر با نسخه URL، `updateViaCache:none`، انتظار `ready`، پیام `skipWaiting` و `clients.claim` خودکار فعال می‌شود؛ عیب‌یابی دیگر درخواست رفرش دستی تکراری نمی‌دهد.
8. شش دسترسی جدید برای یکپارچگی گزینه‌ها، قفل سراسری و کنترل خودکار PWA اضافه شد.

# نسخه ۱۱.۲۷.۰

1. مجوز تب «وضعیت فاکتور پخش‌ها» برای همه کاربران موجود یک‌بار به حالت قابل مشاهده مهاجرت می‌کند؛ پس از آن مدیر می‌تواند آن را صریحاً تغییر دهد. مسیر کلیک تب نیز مستقیماً setup فیلترها و جدول را دوباره متصل می‌کند.
2. موتورهای قدیمی چیدمان از همان زمان اجرای فایل نهایی مهار می‌شوند و در startup اجازه جابه‌جایی/پنهان‌سازی ندارند؛ فقط اقدام صریح مدیر در ستون‌ها یا طراحی دستی اجازه اجرای layout می‌دهد.
3. snapshot چیدمان دیگر در شروع نسخه، MutationObserver یا beforeunload خودکار ساخته نمی‌شود. فقط اقدام مدیر `CRM_MANAGER_GRID_ORDER_V2` را ذخیره می‌کند؛ چهار قفل قدیمی معتبر نیز حفظ می‌شوند.
4. صفحه دسترسی‌ها به ۲۸ گروه با نام دقیق ۲۸ تب واقعی تبدیل شد و گروه‌های شلوغ «ابزارهای مدیریت (نسخه 11.15/11.16/...)» از نمای نهایی حذف شدند.
5. CRUD نهایی کاربران اضافه شد: ویرایش همان رکورد، کلید همیشه‌نمایان «ذخیره اطلاعات کاربر»، جلوگیری از نام کاربری تکراری و بازسازی `CRM_USERS_AUTH`.
6. نقش ایجاد کاربر فقط سرپرست، نماینده علمی و کارشناس فروش است و انتخاب آن الگوی ذخیره‌شده همان سطح را اعمال می‌کند.
7. کادر سطح آماده فقط یک کشویی سطح و کلید «ذخیره سطح دسترسی» دارد و دیگر در هر رندر حذف/ساخته نمی‌شود؛ لرزش checkboxها و hover حذف شد.
8. حالت طوسی فقط خود فیلد و checkbox مرتبط را طوسی می‌کند؛ لیبل و کل کادر رنگ طوسی نمی‌گیرند.

# نسخه ۱۱.۲۸.۰

1. علت اصلی ناپدیدشدن تب‌ها رفع شد: ورود قدیمی فقط `crmUserId` را ذخیره می‌کرد اما موتور دسترسی نام/نقش را می‌خواند. اکنون شناسه، نام کامل، نام کاربری و نقش همگی در نشست ذخیره و کاربر همیشه ابتدا با شناسه پایدار پیدا می‌شود.
2. موتور مرکزی و برگشت‌پذیر دسترسی برای هر ۲۸ تب اضافه شد؛ تیک اصلی هر تب واقعاً منو و صفحه را نمایش/مخفی می‌کند و نبود کلید قدیمی به معنی مجاز است، نه مخفی.
3. ریزدسترسی‌های رایج فرم، لیست، حذف، اکسل، ورود فایل، جزئیات فاکتور، دیتابیس و کاربران نیز روی کنترل‌های واقعی اعمال می‌شوند و MutationObserver کنترل‌های تازه‌رندرشده را پوشش می‌دهد.
4. پنج موتور قدیمی دسترسی دیگر برنده نهایی نیستند؛ `applyUserRolePermissions` و `applyFieldPermissions` به موتور مرکزی نهایی متصل شدند.
5. تمام کلیدهای layout قدیمی `CRM_DOM_FIELD_ORDER_LOCK_V1` از مسیر بازیابی حذف شدند؛ فقط snapshot صریح مدیر در `CRM_MANAGER_GRID_ORDER_V2` معتبر است.
6. علاوه بر `applyFullFormLayout`، دو موتور `applyAllFormLayouts` و `applyCustomFieldOrderInForm` نیز در startup مسدود و فقط هنگام اقدام مستقیم مدیر مجاز شدند.
7. فیلدی که طبق تنظیم مدیر حذف/مخفی نشده باشد، پس از رندرهای قدیمی دوباره قابل مشاهده می‌شود؛ فیلدهای عمداً حذف‌شده یا showInForm=false همچنان محترم‌اند.
8. dropdown و کادر اطلاعات با overflow visible، focus z-index و z-index نهایی بالای کارت‌های بعدی نمایش داده می‌شوند و پشت کادر زیرین پنهان نمی‌شوند.
9. GPS هدف دقت ۱۰ متر را حفظ می‌کند، اما شرط نمونه دوم حذف و سقف انتظار از ۳۰ به ۱۵ ثانیه کاهش یافت؛ دریافت آدرس هم‌زمان با GPS آغاز می‌شود تا زمان‌ها روی هم جمع نشوند.

# نسخه ۱۱.۲۹.۰

1. گراف پروژه پیش از تغییر خوانده شد و قانون آرشیو پایان هر چت دوباره تثبیت شد.
2. مخفی‌سازی قدیمی تب وضعیت فاکتور از لایه v11 حذف شد تا فقط موتور مرکزی نهایی درباره دسترسی آن تصمیم بگیرد و رقابت دو موتور باعث نمایش لحظه‌ای نشود.
3. مهاجرت دسترسی جدید `invoiceStatusPermissionV1129` یک‌بار مجوز تب وضعیت فاکتور را برای کاربران/الگوهای موجود فعال می‌کند؛ پس از آن تغییر صریح مدیر معتبر است.
4. قانون قطعی ارقام انگلیسی در زمان parse نصب می‌شود: Number با locale فارسی نیز en-US تولید می‌کند و خروجی Date فارسی فقط ارقامش انگلیسی می‌شود، بدون تغییر تقویم/متن فارسی.
5. متن گزینه‌های select، placeholder ورودی‌ها، titleها و DOMهای تازه‌رندرشده نیز به ارقام انگلیسی تبدیل می‌شوند؛ password دست‌نخورده می‌ماند.
6. خروجی Excel اطلاعات فروش پخش‌ها برای فیلتر ماه/روز به‌جای `01..12` نام فارسی ماه مثل «مرداد» را می‌نویسد؛ سال و روز با ارقام انگلیسی باقی می‌مانند.

# نسخه ۱۱.۳۰.۰

1. تب وضعیت فاکتور در منو همیشه ثابت و قابل مشاهده است و دیگر هیچ permission engine اجازه حذف دکمه/صفحه آن را ندارد. اگر دسترسی کاربر خاموش باشد، خود تب می‌ماند و داخل آن پیام عدم دسترسی نشان داده می‌شود؛ مهاجرت V1130 کاربران فعلی را یک‌بار مجاز می‌کند.
2. CSS موبایل تا عرض ۹۵۰ پیکسل برای Android/iOS بازطراحی شد: کلیدها wrap می‌شوند، عرض از viewport خارج نمی‌شود و کلیدهای «فرم ثبت/لیست» در دو ستون داخل صفحه می‌مانند.
3. حالت landscape موبایل شبکه دو ستونه، منوی همبرگری، نقشه کوتاه‌تر و پنل کناری متناسب دارد؛ portrait همچنان یک‌ستونه و واضح است.
4. لینک نشان به مسیر universal `nshn.ir/maps?destination=...&type=drive`، بلد به `/directions/driving?destination=lng,lat`، Google به `maps/dir` و Waze به `navigate=yes` تغییر کرد؛ لینک مقصد مسیر را می‌سازد و به‌جای صفحه دانلود از universal link استفاده می‌کند.
5. نماینده علمی بر اساس `crmUserId/repId` و نام خودش فقط داروخانه‌ها، پزشکان و سفارشات مالکیت‌دار خودش را می‌بیند؛ رکورد بدون مالک به کاربر عادی نشت نمی‌کند. فقط مجوز صریح `*_all_reps=true` یا مدیر امکان مشاهده همه را می‌دهد.
6. رکوردهای جدید داروخانه، پزشک، سفارش و فعالیت علاوه بر repName، repId نیز ذخیره می‌کنند.
7. برای تغییر لینک/دامنه، نصب کاملاً خالی فقط وقتی هیچ state محلی ندارد از `/api/state` مقداردهی اولیه می‌شود؛ state موجود هرگز بازنویسی نمی‌شود و save پیش از پایان بررسی لینک مسدود است.
8. فایل‌های حجیم Excel/اسنپ/پخش علاوه بر IndexedDB در endpoint جدید `/api/bulk` و فایل gitignored `user-bulk-data.json` نگهداری و در لینک جدید بازیابی می‌شوند.
9. سرور از `CRM_DATA_DIR` یا دیسک `/var/data` برای `user-data.json` و `user-bulk-data.json` استفاده می‌کند و فایل‌های قبلی ریشه را یک‌بار مهاجرت می‌دهد.

# نسخه ۱۱.۳۱.۰

1. قبل از نمایش برنامه، build جدید با `CRM_ASSET_BUILD` تشخیص داده می‌شود؛ در تغییر نسخه فقط cacheهای asset و service-workerهای قدیمی پاک، build ثبت و صفحه با query جدید یک‌بار باز می‌شود. LocalStorage و IndexedDB اطلاعات کاربر هرگز پاک نمی‌شوند.
2. HTML/JS/CSS از سرور با `no-store/no-cache/max-age=0` و هدرهای CDN/Surrogate بدون کش ارسال می‌شوند؛ SW برای navigation/JS/CSS از request با `cache:no-store` استفاده و پیام purgeOldCaches را پشتیبانی می‌کند.
3. بدنه تا پایان تشخیص نشست، بارگذاری state و اعمال دسترسی مخفی است؛ نماینده/سرپرست/کارشناس دیگر حتی لحظه‌ای صفحه مدیر را نمی‌بیند.
4. تب وضعیت فاکتور همچنان pinned است و V1131/V1130 permission shell آن را از منو حذف نمی‌کند.
5. موبایل: ردیف کالای سفارش در portrait/landscape به شبکه ۴ ستونه کنترل‌شده تبدیل شد؛ نام کالا کامل، تعداد/جایزه، قیمت/جمع و دو کلید ۴۰px دیده می‌شوند. کلید پاک‌کردن فرم حداکثر ۱۳۰px و دیگر غول‌آسا نیست.
6. کلید همبرگری در هر دو جهت با عرض ثابت ۴۴px و z-index بالا نمایش داده می‌شود و drawer افقی تا ۷۲vw داخل صفحه است.
7. Android برای نشان و بلد از Intent مستقیم با package واقعی و fallback universal استفاده می‌کند؛ iOS/وب از Universal Directions. مقصد و حالت رانندگی در لینک حفظ می‌شود.
8. `state.reps` از `state.users` بازسازی می‌شود؛ کاربران حذف‌شده فوراً حذف و کاربران جدید اضافه می‌شوند. selectorهای نماینده برای کاربر عادی فقط خود او و برای مدیر/مجوز all-reps فهرست فعال را نشان می‌دهند.
9. مدیر در فرم کاربر استان، شهر و مناطق مسیر فعالیت را تعریف می‌کند؛ مسیر کنار نام کاربر/نماینده مانند «تهران (مناطق 1، 2، 3)» نمایش و در selectorها نیز دیده می‌شود.
10. حذف کاربر در لایه نهایی، auth، reps و همه selectorهای نماینده را همان لحظه همگام می‌کند.

# نسخه ۱۱.۳۲.۰

1. هدرهای امنیتی سراسری اضافه شد: Content-Security-Policy، Permissions-Policy، HSTS، nosniff، strict referrer، same-origin opener/resource، ممنوعیت cross-domain policy و DNS prefetch.
2. دسترسی‌های حساس سیستم/گوشی از سمت مرورگر مسدود شدند: دوربین، میکروفون، USB، Serial، HID، Bluetooth، Payment، ضبط صفحه، شتاب‌سنج، ژیروسکوپ و مغناطیس‌سنج. فقط geolocation همان origin با اجازه مستقیم کاربر مجاز است.
3. CORS wildcard کاملاً حذف شد. درخواست POST به API فقط با Origin/Sec-Fetch معتبر و هدر `X-CRM-Request: 1` پذیرفته می‌شود؛ درخواست cross-site یا بدون هدر 403 می‌گیرد.
4. JSON ورودی state/bulk/email عمقی پاک‌سازی می‌شود: prototype/constructor pollution، عمق بیش از ۴۰، مقادیر غیرمتناهی، control characters و کلیدهای بسیار بلند رد/پاک می‌شوند.
5. نوشتن `user-data.json` و `user-bulk-data.json` اتمی (temporary+rename) و با مجوز فایل 0600 است تا خرابی نیمه‌نوشتن و دسترسی سیستم کاهش یابد؛ خواندن فایل خراب، سرور را crash نمی‌کند.
6. محافظ مرورگر فقط پروتکل http/https را برای window.open می‌پذیرد و `noopener,noreferrer` اجباری است؛ javascript/data/file/vbscript link مسدود می‌شود.
7. فایل‌های اجرایی/نصبی/اسکریپتی مثل exe, msi, apk, bat, cmd, ps1, vbs, js, jar, sh, dmg, pkg, app, iso, dll و فایل بیش از ۳۲MB پیش از پردازش مسدود می‌شوند. فقط داده/تصویر مجاز پذیرفته می‌شود.
8. ورودی متنی از تگ‌های `< >` و کنترل‌کاراکترهای خطرناک پاک می‌شود؛ لینک پنجره جدید rel امن می‌گیرد؛ drag/drop فایل خطرناک متوقف می‌شود.
9. Excel HTML در برابر Formula Injection محافظت شد؛ مقادیر آغازشونده با `=`, `+`, `@` یا منفی غیرعددی به متن امن تبدیل می‌شوند.
10. URL پنل شرکت پخش فقط HTTPS معتبر است؛ نام کاربری از تگ HTML پاک می‌شود.
11. کادر «وضعیت حفاظت دستگاه و داده» به عیب‌یابی اضافه شد و شش ریزمجوز امنیتی در دسترسی‌ها ثبت شدند.

# نسخه ۱۱.۳۳.۰

1. نام کالا در ردیف سفارش از حالت datalist/کشویی خارج و به input ساده readonly با نام ثابت کاتالوگ تبدیل شد؛ refresh کاتالوگ دیگر attribute list را برنمی‌گرداند.
2. listener نهایی تعداد/جایزه/قیمت روی `orderItemsContainer` اضافه شد تا جمع هر ردیف و جمع سفارش با هر ورودی فوراً محاسبه شود؛ فیلد جمع مبلغ در موبایل display/visibility قطعی دارد.
3. کلیدهای ویرایش و حذف هر ردیف کالا فقط برای مدیر نمایش داده می‌شوند و برای نماینده، سرپرست و کارشناس فروش مخفی‌اند.
4. privacy wrapper تمام renderهای فعالیت لحظه‌ای، مرخصی، مسیرها، منزل، گزارش ماهانه و تارگت را بر اساس repId/repName فیلتر می‌کند؛ v20 routes/targets و activity map نیز مستقیم privacyList دارند.
5. selectorهای نماینده بعد از هر رندر از users فعال بازسازی و برای کاربر عادی فقط خود او را نشان می‌دهند؛ حذف کاربر auth/reps/selectors را هم‌زمان تازه می‌کند.
6. نسخه 11.31 cache gate، pinned invoice shell، mobile grid، مسیر فعالیت و native navigation بدون تغییر حفظ و با تست‌های جدید ضد‌رگرسیون پوشش داده شدند.

# نسخه ۱۱.۳۴.۰

1. کادر «تعریف مسیرهای نمایندگان» در بالای تب تارگت فروش اضافه شد: نماینده علمی + انتخاب چندگانه استان، شهر و منطقه + کلید ذخیره مسیر.
2. گزینه‌های استان/شهر/منطقه از IRAN_GEO_DATA، geoExtras و registry استاندارد لیبل‌ها خوانده می‌شوند. انتخاب استان، شهرها و انتخاب شهر، مناطق را تازه می‌کند.
3. مسیرها به‌صورت آرایه‌های activityProvinces/activityCities/activityDistrictList روی user ذخیره و routeLabel کنار نام نماینده و در selectorها نمایش می‌شود.
4. سه فیلد مسیر فعالیت از فرم کاربران و دسترسی حذف شد؛ داده قدیمی حذف نمی‌شود و در اولین ذخیره مسیر جدید مهاجرت/ادامه می‌یابد.
5. فرم قدیمی تک‌کالای تارگت مخفی و برنامه‌ریز جدولی جدید اضافه شد: نماینده/سال/ماه در یک سطر و همه کالاهای ثابت زیرهم با تعداد تارگت، قیمت واحد پخش/داروخانه و جمع‌های ضرب‌شده.
6. جمع کل مبلغ پخش و داروخانه زیر ردیف‌ها نمایش و ذخیره همه تارگت‌های همان نماینده/سال/ماه با یک کلید انجام می‌شود.
7. گزارش اول جمع همه نمایندگان به تفکیک کالا و سپس کارت جداگانه هر نماینده را با تارگت، محقق‌شده، مانده، قیمت‌ها و جمع مبالغ نشان می‌دهد.
8. متن خراب CSS زیر توضیحات مرخصی حذف و formLeaveRequest صحیح بازسازی شد.
9. در منزل نمایندگان کلید ویرایش و حذف کنار نمایش روی نقشه اضافه شد.
10. افزودن لحظه‌ای گزینه برای نماینده علمی، سال، ماه، استان، شهر، منطقه و نام داروخانه در فرم‌ها مسدود شد؛ تغییر گزینه‌های مرجع فقط از مدیر افزودن‌ها ممکن است.
11. پس از جایگذاری داروخانه در سفارش، dataset ثابت و observer مانع بازگشت پیام «قبلاً ثبت شده — انتخاب» و کادر انتخاب قبلی می‌شود؛ با تغییر واقعی نام، قفل برداشته می‌شود.
12. privacyList حتی برای مدیر، رکوردهای نمایندگانی را که دیگر در users فعال نیستند از نمای گزارش حذف می‌کند؛ تاریخچه canonical پاک نمی‌شود.

# نسخه ۱۱.۳۵.۰

1. قانون جدید تحویل چندبخشی با فایل `AI_ACCEPTANCE_CHECKLIST.md` پیاده شد: هر بند درخواست، تغییر واقعی، تست، وضعیت و runtime check پیش از ZIP ثبت می‌شود.
2. علت واقعی ناپدیدشدن تب فاکتور در اجرای JSDOM پیدا شد: `setupNavigationMenu` منو را از `MENU_SECTIONS_LIST` بازسازی می‌کرد و این لیست فقط ۲۷ تب داشت؛ invoice-status به منبع اصلی منو اضافه و runtime تعداد ۲۸ تب را تأیید کرد.
3. اعلان عمومی فقط برای مدیر یا کاربر دارای `notify_all_users=true` در گیرنده‌ها دیده می‌شود؛ فهرست گیرنده‌ها idempotent است تا global option engine آن را برنگرداند.
4. پیام‌ها threadId/parentId دارند؛ کلید پاسخ و تاریخچه کنار هر پیام اضافه شد و کل رفت‌وبرگشت sender→recipient با تاریخ حفظ می‌شود.
5. Web Push بدون dependency اضافه پیاده شد: VAPID P-256 خودکار، AES128GCM encryption، subscription persistence، send endpoint، stale cleanup و Service Worker push/notificationclick.
6. کلید «فعال‌سازی اعلان صدادار دستگاه» اضافه شد؛ با اجازه کاربر، اعلان با vibration/renotify/requireInteraction حتی در حالت بسته بودن PWA از Push Service دریافت می‌شود. صدا تابع تنظیمات سیستم‌عامل است.
7. قفل سفیدشدن سفارش رفع شد: MutationObserver پیام داروخانه فقط در صورت تغییر واقعی style/hidden می‌نویسد و دیگر روی mutation خودش حلقه بی‌نهایت نمی‌سازد.
8. فیلد نماینده در پروفایل کاربر عادی به select تک‌گزینه‌ای disabled با ظاهر ساده و نام ثابت تبدیل شد؛ افزودن گزینه مرجع همچنان ممنوع است.
9. Runtime validation واقعی JSDOM همه اسکریپت‌ها را از سرور بارگذاری کرد و invoice، target، محاسبه order total، permission public option، reply/history و نبود خطای runtime را تأیید کرد.

# نسخه ۱۱.۳۶.۰

1. قفل ترتیب فرم سفارش تقویت شد: کادرهای دارای شناسه ولی بدون input داخلی نیز با خود شناسه کادر در snapshot شناخته می‌شوند؛ بنابراین `orderCustomFieldsContainer` دیگر هنگام پاک‌کردن به ابتدای فرم نمی‌پرد.
2. پاک‌کردن فرم سفارش قبل از اجرا ترتیب DOM و تنظیمات `formFieldMeta.order/customFields.order` را در حافظه موقت می‌گیرد و پس از بازسازی فرم بدون تغییر داده/تنظیمات همان ترتیب را بازمی‌گرداند.
3. محافظ درون‌حافظه‌ای فرم سفارش، جابه‌جایی دیرهنگام کدهای قدیمی را برمی‌گرداند و فقط اقدام صریح مدیر را به‌عنوان ترتیب تازه می‌پذیرد؛ هیچ snapshot خودکار تازه‌ای در LocalStorage نوشته نمی‌شود.
4. پس از پرکردن خودکار داروخانه، پیام «قبلاً ثبت شده» و کادر انتخاب با `hidden`، `aria-hidden` و `display:none!important` به‌صورت idempotent مخفی می‌مانند و فقط با تغییر واقعی نام آزاد می‌شوند.
5. `setupRepresentativeRoutes` اکنون در boot، ورود به تب تارگت و reliable boot فراخوانی می‌شود؛ فهرست نمایندگان و ۳۱ استان و فیلدهای شهر/منطقه واقعاً ساخته می‌شوند.
6. اسکنر عنوان فیلد، label مستقیم هر form-group را حتی بدون attribute `for` می‌خواند. عنوان فنی ذخیره‌شده مانند `leaveRepSelect` برای builtinها رد و عنوان فارسی DOM/registry جایگزین می‌شود.
7. فعالیت لحظه‌ای renderer نهایی اختصاصی دارد: مدیر فقط فعالیت کاربران فعال را می‌بیند و نماینده حتی با مجوز قدیمی all-reps فقط فعالیت خودش را می‌بیند؛ داده تاریخی حذف نمی‌شود.
8. تست مرورگرمانند واقعی تأیید کرد: route rep=3/province=31، عنوان مرخصی «نماینده علمی»، پیام جایگذاری مخفی، تنظیمات و ترتیب سفارش پس از پاک‌کردن ثابت، کاربران حذف‌شده نامرئی و نماینده self-only است.

# نسخه ۱۱.۳۷.۰

1. قبل از تغییر، خروجی خواندنی state/bulk نسخه 11.36.0 و manifest هش در backup خصوصی `.arena` ثبت شد. مسیر ارتقا برای state موجود هیچ پاک‌سازی، merge یا default injection ندارد.
2. نصب کاملاً تازه دیگر کاربران و داده‌های نماینده نمونه قدیمی را ایجاد نمی‌کند؛ فقط مدیر اولیه باقی می‌ماند. این قانون فقط وقتی `CRM_APP_STATE_V2` اصلاً وجود ندارد اجرا می‌شود و داده فعلی کاربران را لمس نمی‌کند.
3. حذف کاربر tombstone شامل id/username/fullName ثبت می‌کند؛ tombstone در boot مانع برگشت کاربر از state قدیمی می‌شود و auth/reps/selectors تازه می‌شوند. دو هویت نمونه شناخته‌شده `u-2/Taheri/جواد علمدار` و `u-3/nila/خانم نیلا محرمی` که مدیر قبلاً حذف کرده، یک‌بار به tombstone مهاجرت می‌شوند تا از state نمونه قدیمی هم برنگردند.
4. انتخاب داروخانه سفارش handler نهایی delegated دارد؛ هر کارت تازه‌ساخته‌شده با یک کلیک matchedId و همه اطلاعات را جایگذاری و پیام duplicate را پنهان می‌کند.
5. فیلدهای تعریف مسیر برای مدیر system-pinned شدند تا metadata قدیمی hidden نتواند نماینده/استان/شهر/منطقه و ذخیره را پنهان کند؛ state metadata تغییر داده نمی‌شود.
6. محافظ نهایی عنوان builtin هر شناسه فنی انگلیسی را در خروجی `getUnifiedFieldList` با label فارسی DOM/registry جایگزین می‌کند، بدون بازنویسی metadata ذخیره‌شده.
7. منزل نمایندگان renderer نهایی دارد: مدیر منزل همه کاربران فعال و نماینده فقط منزل خود را در جدول و لایه‌های نقشه می‌بیند؛ markerهای غیرمجاز از نقشه حذف می‌شوند. رکورد جدید repId ذخیره می‌کند.
8. مرخصی ساعتی به دو فیلد مستقل time «از ساعت» و «تا ساعت» تبدیل شد؛ fromTime/toTime جدا ذخیره و رشته legacy hours برای سازگاری حفظ می‌شود.
9. گیرنده اعلان plain select شد (`data-nocombo=1`)، input تایپی/افزودن گزینه حذف و placeholder + کاربران فعال ساخته می‌شود.
10. فلش کشویی‌های جستجویی z-index/pointer area قطعی و delegated capture handler دارد؛ کلیک فلش مستقیماً فهرست را باز/بسته می‌کند.
11. Runtime واقعی با manager/representative و sentinel نتیجه داد: preserved=true، route 4 fields/31 provinces، picker selected/filled/hidden، times=true، arrow=true، notification plain، manager homes all-active، rep home/activity self-only، ghost removed، errors=[].

# نسخه ۱۱.۳۸.۰

1. موتور مستقل `/cache-reset` اضافه شد: پاسخ با `Clear-Site-Data: "cache"` فقط cache را پاک می‌کند، همه CacheStorage و Service Workerهای قدیمی را unregister می‌کند و خودکار به همان صفحه با query یکتای build/timestamp برمی‌گردد.
2. موتور هرگز LocalStorage، IndexedDB، CRM_APP_STATE_V2، customFields، formFieldMeta یا داده‌های Excel را پاک نمی‌کند. Runtime sentinel حفظ کامل state/settings را تأیید کرد.
3. index و login در هر بار ورود `/api/health?__crm_nocache=timestamp` را با `cache:no-store` می‌خوانند؛ اگر نسخه سرور و HTML متفاوت باشد، خودکار وارد cache-reset می‌شوند، حتی وقتی HTML قدیمی و CRM_ASSET_BUILD قدیمی با هم برابر باشند.
4. HTML همیشه `Clear-Site-Data: "cache"`، no-store و `X-CRM-Build` دارد؛ sw.js و health نیز no-store/X-CRM-Build هستند.
5. Service Worker جدید در install/activate همه cacheها را پاک، فوراً skipWaiting/claim و build فعال را به پنجره‌ها broadcast می‌کند.
6. navigation و HTML/JS/CSS/JSON/WebManifest در SW فقط از شبکه با cache reload/no-store می‌آیند و هیچ fallback به asset قدیمی ندارند؛ در قطع اینترنت پیام تلاش دوباره نمایش داده می‌شود.
7. crm-app پیام CRM_BUILD_ACTIVE را می‌شنود و در mismatch خودکار cache-reset را اجرا می‌کند؛ این معادل عملی hard reload است و نیاز به Ctrl+Shift+R دستی را حذف می‌کند.
8. HTTP واقعی headerها و endpoint، runtime حذف ۲ cache/۲ SW با حفظ sentinel، ۵۷/۵۷ تست، build و syntax تأیید شد.

## تحویل مستندات نوبت ۶۰ — بدون تغییر نسخه برنامه

1. فایل `GITHUB_REVIEW_HANDOFF.md` ساخته شد تا GitHub reviewer و هوش مصنوعی بعدی وضعیت واقعی source/commit/push/PR/deploy/production را یکجا ببیند.
2. README در ابتدای فایل به handoff و PROJECT_GRAPH ارجاع می‌دهد و روشن می‌کند runtime واقعی `server.js + public/` است، نه اسکلت تاریخی Next.js.
3. نتیجه تکراری push و Create Ref ثبت شد: GitHub App فاقد Workflows/Refs permission است و 403 می‌دهد؛ شاخه Arena remote نشده است.
4. production دوباره بررسی و نسخه 11.20.0 ثبت شد؛ نسخه 11.38.0 تا push/PR/merge/deploy آنلاین نیست.
5. نقشه مویرگی cache rescue، state/bulk، order, route, labels, tombstones, privacy, leave, notifications, Web Push، workflow و تست‌ها در handoff آمده است.

## تحویل نوبت ۶۱ — تأیید انتشار 11.38.0، ممیزی ۲۰ چت اخیر و همگام‌سازی کامل آرشیو (بدون تغییر کد برنامه)

1. وضعیت واقعی دوباره اندازه‌گیری شد: working tree clean، remote فقط `main` در `f541301`، GitHub App لاگین ولی با REST API محدود؛ push کامیت بدون تغییر workflow مجاز است.
2. شاخه `arena/01a0262d-javad-test1` با موفقیت به origin push شد؛ PR لازم نبود چون نوک شاخه دقیقاً برابر نوک main بود (سورس 11.38.0 از قبل روی main) و GitHub پیام «No commits between» داد.
3. workflow «Build & Mirror & Deploy» روی `f541301` تکمیل موفق داشت (build + 58 تست).
4. کاربر production فعال را `https://javad-test1.onrender.com` اعلام کرد؛ `/api/health` دو بار مستقل نسخه `11.38.0` را برگرداند (2026-08-21T21:24:15Z) و `/panel` با redirect زنده `/login?build=11.38.0&__crm_reload=...` موتور cache rescue را در عمل نشان داد؛ صفحه ورود «طنین طب طاها» سالم بالا آمد.
5. ممیزی پرامپت‌های نوبت‌های ۴۱ تا ۶۰ (نسخه‌های 11.23.0 تا 11.38.0) با ۲۲ نشانگر کد انجام شد: SHAFA_CODE_MAP اصلاحی (1001→1391911001 تا 1007→1391902006)، userEditId، safeOrderFields، سرستون #87CEEB، ensureProductCodes، تب وضعیت فاکتور پخش‌ها، invoiceStatusBaseCache، globalFieldOptions، CRM_MANAGER_GRID_ORDER_V2، syncRepsFromUsers، Permissions-Policy، x-crm-request، privacyList، representativeRoutesCard، setupTargetPlannerV34، notify_all_users، vapidAuthorization، groupAnchor، deletedUserTombstones، leaveFromTime، /cache-reset و CRM_BUILD_ACTIVE — همه در سورس حاضر و 58/58 تست موفق؛ هیچ پرامپت معلق کدی نماند.
6. تصمیم: نسخه برنامه 11.38.0 بدون تغییر می‌ماند؛ bump بدون تغییر واقعی کد ممنوع است (قوانین ۸۹ و ۹۱).
7. قانون جدید دائمی ۹۱ در AI_RULES ثبت شد: در هر چت همه ۱۵ فایل آرشیوی به‌روز شوند، PROJECT_GRAPH با update_project_graph.py و chat.arena با update_chat_arena.py بازسازی شوند و ZIP طبق قانون ۶۴ کنار صفحه بیاید.
8. همه آرشیوها به‌روز شدند: GITHUB_REVIEW_HANDOFF (بخش ۰ وضعیت انتشار)، AI_ACCEPTANCE_CHECKLIST (نوبت ۶۱)، AI_DECISION_LOG (تصمیم ۹۳)، AI_TASKS (بخش‌های ۸۱ و ۸۲)، AI_PROJECT_CONTEXT، ARENA_HANDOFF_PROMPT (شروع اجباری بعد از نوبت ۶۱)، README، این فایل؛ graph و chat.arena 1.56 بازسازی شدند.
9. GitLab mirror تنها حلقه تأییدنشده باقی مانده است؛ سرویس قدیمی namayandeelmi-javad.onrender.com هنوز 11.20.0 است و دیگر مرجع health نیست.

## تحویل نوبت ۶۲ — قانون اجرای جزبه‌جز + راستی‌آزمایی ۲۱ بندِ پرامپت کاربر (بدون تغییر کد برنامه)

1. قانون دائمی ۹۲ به AI_RULES اضافه شد: هر پرامپت به بندهای شماره‌دار شکسته شود، هر بند واقعاً در سورس اعمال/راستی‌آزمایی شود، و قبل از ZIP هوش مصنوعی خودش با سرور واقعی و تست ورود خودکار (app-smoke) وارد برنامه شود؛ تحویل فقط با جدول شواهد کامل.
2. قالب بلوک پایان چت دائمی شد: git add -A / git commit با شماره نسخه و خلاصه فارسی / git push origin main / git push gitlab main.
3. پرامپت بلند کاربر (اعلان، پاسخ/تاریخچه، Web Push صدادار، سفارشات/داروخانه، فعالیت لحظه‌ای، منزل نمایندگان، حفظ اطلاعات، قفل فیلدها، مسیرهای تارگت، label فارسی، مرخصی ساعتی، گیرنده plain و فلش کشویی‌ها) بند‌به‌بند راستی‌آزمایی شد: همه در 11.23.0–11.38.0 حاضر بودند؛ شواهد دقیق (نام تابع و خط) در AI_ACCEPTANCE_CHECKLIST نوبت ۶۲ ثبت شد.
4. نکات کلیدی شواهد: گزینه اعلان عمومی فقط با notify_all_users (v20:1327)؛ کلید پاسخ/تاریخچه در renderNotificationsV35/showNotificationThreadV35؛ کارت داروخانه delegated در bindOrderPharmacyCardV37 (v20:1338)؛ حذف قطعی جواد علمدار/نیلا محرمی با tombstone (v20:1331)؛ منزل نماینده privacy در visibleRepHomesV37؛ «از ساعت/تا ساعت» فیلدهای ۵ و ۶ با step=60؛ گیرنده plain و فلش کشویی در setupV37FinalGuards.
5. ورود خودکار به برنامه: سرور واقعی spawn شد و app-smoke ۴/۴ پاس شد (health/HTML/اسکریپت‌ها/UI حیاتی + round-trip حفظ sentinel و vault اکسل + VAPID کلید عمومی و رد subscription خراب)؛ مجموع کامل ۵۸/۵۸.
6. هیچ بند معلق کدی نماند؛ طبق قوانین ۸۹/۹۱ نسخه برنامه بدون تغییر واقعی bump نشد و 11.38.0 ماند؛ آرشیوها/graph/chat 1.56 بازسازی و ZIP با سرور دانلود پورت 8000 تحویل شد.

## تحویل نوبت ۶۴ — نسخه ۱۱.۳۸.۱: تعویض کامل دامنه قدیمی با javad-test1.onrender.com

1. کاربر تصریح کرد سرویس فعال javad-test1.onrender.com با نشان 11.38.0 است؛ دستور تبدیل هر رد دامنه قدیمی که در برنامه ثبت شده شد.
2. تمام ردهای `namayandeelmi-javad.onrender.com` در ۱۰ فایل کد/کانفیگ به `https://javad-test1.onrender.com` تبدیل شد: deploy.yml (sync)، keep_alive.yml (اصلاح مهم: بیدارباش سرویس فعال به‌جای قدیمی)، .gitlab-ci.yml، render.yaml، mobile/src/simAuth.ts، public/crm-data.js (apiEndpointUrl)، public/crm-app.js (متن عیب‌یابی)، src/lib/endpoints.ts، src/lib/sync-config.ts، src/proxy.ts — صفر رد باقی ماند.
3. خلل‌سنجی: فراخوانی‌های /api/state از قبل نسبی بودند و چک same-origin سرور هاست‌محور است؛ apiEndpointUrl نمایشی/فال‌بک بود؛ خلل عملیاتی واقعی keep_alive بود که سرویس فعال را خواب می‌گذاشت — اصلاح شد. اپ موبایل هم اکنون به سرویس فعال وصل است.
4. چون تغییر واقعی کد بود، نسخه به 11.38.1 ارتقا یافت: package.json، server.js، public/sw.js، public/index.html (BUILD + ?v=)، public/login.html، public/crm-app.js (register sw ?v=) و انتظارات تست نسخه همگام شدند.
5. اثبات زنده کد 11.38 روی سرویس فعلی ثبت شد: /api/push/public-key فعال، /cache-reset فعال، نشان نسخه هدر 11.38.0.
6. آزمون‌ها: 58/58 تست، syntax همه JS، app-smoke سرور واقعی 4/4 با 11.38.1؛ آرشیوها و chat.arena 1.57 بازسازی؛ ZIP namayandeelmi-v11.38.1.zip با سرور دانلود پورت 8000 تحویل شد؛ push به GitHub از این جلسه بسته است و با بلوک دستور پایان چت توسط کاربر انجام می‌شود.

## تحویل نوبت ۶۵ — نسخه ۱۱.۳۹.۰: ترتیب کانونی سفارشات، لینک/مخزن داینامیک و کارایی

1. فرم سفارشات ترتیب کانونی جدید گرفت: «تاریخ سفارش» اول، بعد «نام داروخانه»، استان، شهر، منطقه و سپس بقیه (index.html)؛ و مهاجرت یک‌باره v39OrdersCanonicalReset ترتیب ذخیره‌شده به‌هم‌ریخته مدیر را یک‌بار با همین ترتیب کانونی جایگزین می‌کند (کلید CRM_V39_ORDER_CANONICAL_RESET فقط یک‌بار اجرا می‌شود).
2. داینامیک شدن لینک/مخزن طبق درخواست: apiEndpointUrl از location.origin ساخته می‌شود؛ متن عیب‌یابی هاست فعال را از location.host می‌گیرد؛ API_BASE موبایل با override (globalThis.__CRM_API_BASE__) و fallback سرویس فعال — ساخت هر مخزن/لینک Render جدید بدون تغییر کد کار می‌کند.
3. کارایی: علت «۱۰ دقیقه لود» خواب سرویس رایگان Render بود؛ keep-alive از نوبت ۶۴ سرویس فعال را بیدار نگه می‌دارد؛ کل دارایی‌ها 1.34MB خام → 327KB gzip با gzip فعال سرور و اسکریپت‌های انتهای body؛ پس از بیداری، لود ثانیه‌ای است؛ برای بیداری دائم UptimeRobot روی لینک فعال توصیه شد.
4. تأیید با سند کد: تارگت به تفکیک هر کالا (ماتریس «تعریف تارگت کالاها» v34 با ردیف هر کالا و جمع‌ها) و قانون اعداد لاتین (installLatinNumberLaw) هر دو حاضرند؛ بندهای تکراری نوبت ۶۲ دوباره راستی‌آزمایی شدند.
5. نسخه 11.39.0 با تغییر واقعی کد اعمال شد (شش فایل نسخه + انتظارات تست)؛ تست رگرسیون جدید ترتیب/مهاجرت/داینامیک اضافه شد؛ 59/59 تست، syntax همه JS و app-smoke سرور واقعی 4/4 موفق؛ ZIP namayandeelmi-v11.39.0.zip با سرور دانلود تحویل شد.

## تحویل نوبت ۶۷ — نسخه ۱۱.۴۰.۰: ریشه‌یابی واقعی چهار گزارش زنده کاربر

1. حذف دائمی «جواد علمدار» و «خانم نیلا محرمی» از همه تب‌ها: علت، نمونه‌های داده اولیه و تطابق دقیق مهاجرت tombstone بود. موتور جدید enforceRemovedIdentitiesV40 در هر راه‌اندازی، این هویت‌ها را (با نام نرمال/username/id) از users حذف و dropRemovedUserRowsV40 ردیف‌های آن‌ها را از leaves/مسیرها/منازل/تارگت‌ها/گزارش ماهانه کنار می‌گذارد؛ تاریخچه canonical حفظ می‌شود و نماینده فقط فعالیت خودش را می‌بیند.
2. گیرندگان اعلان نقش‌آگاه شد: مدیر همه کاربران فعال را به تفکیک دارد؛ غیرمدیر فقط مدیر/سرپرست/کارشناس فروش.
3. انتخاب داروخانه در سفارشات سه‌مسیره شد (click + mousedown + pointerdown با dedupe) و بازخورد toast دارد («جایگذاری شد» / «پیدا نشد»)؛ پیغام «قبلاً ثبت‌شده» پس از جایگذاری به‌صورت idempotent پنهان می‌ماند و گزینه افزودن برای نام داروخانه وجود ندارد.
4. افزودن گزینه برای فیلدهای مرجع (نماینده علمی/سال/ماه/استان/شهر/منطقه/نام داروخانه) برای غیرمدیر مسدود و پنهان شد (installReferenceInstantAddGuardV40)؛ فقط مدیر می‌تواند زیرمجموعه اضافه/حذف کند.
5. کادر «تعریف مسیرهای نمایندگان» (بالای تب تارگت، کشویی نماینده + چند استان/شهر/منطقه + کلید ذخیره مسیر) موجود و راستی‌آزمایی شد؛ فیلدهای مسیر از تب کاربران حذف شده‌اند.
6. آزمون‌ها: تست رگرسیون جدید هشت‌ادعایی؛ مجموع 60/60؛ ورود خودکار به برنامه (سرور واقعی) 4/4؛ نسخه در شش فایل و انتظارات تست همگام؛ ZIP namayandeelmi-v11.40.0.zip با سرور دانلود پورت 8000 تحویل شد.

## تحویل نوبت ۸۰ — نسخه ۱۱.۶۱.۰: ریشه‌یابی و حذف حلقه‌های خودتغذی MutationObserver؛ فعال‌شدن کامل فیلدهای سه تب

1. گزارش کاربر: «در تب داروخانه‌ها، پزشکان و سفارشات زیرمجموعه فیلدهای کشویی باز می‌شود ولی نمی‌توان گزینه‌ها را انتخاب کرد؛ داخل فیلدها قابل تایپ نیست؛ مدام جای فیلدها عوض می‌شود و در جای خود قفل نیستند».
2. راستی‌آزمایی با اجرای واقعی رابط کاربری (سرور server.js + JSDOM): تب سفارشات پس از ثانیه‌های اولیه وارد طوفان تغییرات DOM دائمی می‌شد — بیش از **۲هزار device mutation در ثانیه** (پنجره‌های ۴ثانیه‌ای: ~۹ تا ۱۰هزار جهش، بدون توقف) که کل رابط را کند و فیلدها را ناپایدار می‌کرد؛ حتی وقتی تب سفارشات فعال نبود این طوفان ادامه داشت.
3. سه حلقه خودتغذی شناسایی و بسته شد: (الف) `v20ReorderListColumns` سلول‌های جدول لیست سفارشات را حتی در ترتیبِ یکسان دوباره appendChild می‌کرد و ناظر `bindListOrderObserver` همان را دوباره فعال می‌کرد؛ (ب) `bindOrderFormPositionLock` با هر تغییر childList داخل فرم (از جمله بازشدن لیست کشویی و تایپ) کل فرم را re-append می‌کرد و پرچم busy به‌صورت همگام قبل از پردازش جهش‌های خودش آزاد می‌شد؛ (ج) `restoreOrderFormSequence` و `restoreDomFieldOrder` بدون قابلیت idempotent بودن، هر بار تمام گروه‌های فرم را جابه‌جا می‌کردند.
4. پنج وصله جراحی در `public/crm-bundle.js` (فایل اجرایی واقعی) و آینه آن در `public/crm-features-v20.js` اعمال شد: جابه‌جایی فقط در صورت تفاوت واقعی ترتیب (`cells[i] !== cells[finalOrder[i]]` و مقایسه currentIds/desiredIds)، ناظر قفل موقعیت فقط برای تغییرات ساختاری واقعی (نادیده‌گرفتن `.crm-combo-list`) با آزادسازی آسنکرون busy پس از ۱۲۰ms، نادیده‌گرفتن جهش‌های داخل لیست کشویی در ناظر دسترسی مرکزی، و idempotent شدن قفل طوسی v49 (`dataset.v49grey`) که هر ۴ ثانیه استایل‌ها را بازنویسی می‌کرد.
5. اصل «فقط تاریخ/داروخانه/اولویت فعال» (بند ۱۰ نسخه ۱۱.۴۹) و زنجیره طوسی استان→شهر→منطقه (v20) دست‌نخورده حفظ شد؛ هیچ رفتار طراحی‌شده‌ای تغییر نکرد، فقط حلقه‌های بازنویسی DOM حذف شدند.
6. انتخاب داروخانه در فرم سفارش (کشویی)، تایپ/فیلتر/انتخاب استان و شهر در هر سه تب، تایپ در نام داروخانه، و کلیک دکمه ثبت (پیام اعتبارسنجی فارسی) به‌صورت End-to-End در محیط اجرایی واقعی تأیید شد؛ پس از وصله، شمارش جهش‌های بیکار به **صفر مطلق** رسید.
7. نسخه به 11.61.0 ارتقا یافت: package.json، server.js، public/sw.js، public/index.html (BUILD + پنج مهر ?v=)، public/login.html، public/crm-app.js (CRM_APP_VERSION + register sw + build) و انتظارات تست‌ها همگام شدند؛ تست رگرسیون جدید «guards v11.61.0» برای هر پنج قفل افزوده شد.
8. آزمون‌ها: ۶۲/۶۲ تست رگرسیون، ۱۶/۱۶ حفظ وضعیت، ۴/۴ ورود خودکار سرور واقعی (مجموع ۸۲/۸۲)؛ syntax هر دو فایل ویرایش‌شده تأیید شد؛ پیش‌نمایش زنده v11.61.0 روی پورت 8000 بالاست؛ ZIP طبق قانون ۶۴ در کنار صفحه تحویل شد.

## تحویل نوبت ۸۱ — نسخه ۱۱.۶۲.۰: نام داروخانه/پزشک فیلد ساده + افزودن لحظه‌ای فقط دو تب + پلاک/طبقه بیرون از کادر درصد

1. گزارش کاربر (چهار بار تکرارشده): در تب داروخانه‌ها و پزشکان، نام داروخانه و نام پزشک باید فیلد ساده باشند؛ افزودن لحظه‌ای فقط در همین دو تب؛ و فیلدهای پلاک و طبقه به‌اشتباه داخل کادر درصد قرار گرفته‌اند.
2. HTML: برچسب نام داروخانه از «انتخاب یا تایپ + افزودن لحظه‌ای» به «نام داروخانه *» ساده شد؛ هر دو فیلد نام `data-simple-name="1"` و بدون datalist شدند. پلاک و طبقه از داخل کادر زرد درصد بیرون آمدند و بعد از آدرس، پیش از کادر درصد قرار گرفتند. کادر درصد فقط بله/خیر دارد (`#pharmacyPercentBox` / `#doctorPercentBox`).
3. منطق: `setupInstantAdd` فقط pharmacyName/doctorName را پوشش می‌دهد و datalist نمی‌سازد؛ `setupInstantAddAll` فقط داخل `#tab-pharmacies,#tab-doctors` اجرا می‌شود و نام‌های ساده/پلاک/طبقه را رد می‌کند؛ `skipInstant` افزودن لحظه‌ای را بیرون از دو تب و روی پلاک/طبقه ممنوع می‌کند.
4. لایه نهایی v62 در انتهای crm-bundle: ساده نگه‌داشتن نام‌ها (حذف list/combo)، بیرون‌کشیدن پلاک/طبقه اگر هنوز در کادر درصد باشند، محدودکردن `attachInstantAdd` به دو تب، و حذف دکمه‌های افزودن لحظه‌ای از تب‌های دیگر (از جمله سفارشات و کالاها).
5. راستی‌آزمایی زنده (سرور 11.62.0 + JSDOM): نام‌ها INPUT بدون list/combo؛ کادر درصد فقط دکمه‌های بله/خیر؛ پلاک/طبقه بیرون از کادر؛ شمار افزودن لحظه‌ای: داروخانه ۸ / پزشک ۶ / سفارشات ۰ / کالا ۰؛ تایپ نام جدید دکمه «➕ افزودن «…»» را نشان می‌دهد بدون باز شدن کشویی.
6. نسخه 11.62.0 در ۷ فایل نسخه + تست رگرسیون جدید؛ مجموع ۸۳/۸۳ تست سبز.

## تحویل نوبت ۸۲ — نسخه ۱۱.۶۳.۰: نام ساده بدون زیرمجموعه؛ حذف افزودن لحظه‌ای از فیلدهای متنی؛ چیدمان ۳ستونه پزشک؛ لیست داروخانه‌های ثبت‌شده در سفارشات

1. نام داروخانه و نام پزشک دیگر هیچ زیرمجموعه‌ای ندارند: بدون datalist، بدون combo، بدون ردیف افزودن لحظه‌ای — فقط فیلد متنی قابل تایپ (`data-simple-name`).
2. افزودن لحظه‌ای از همه فیلدهای متنی حذف شد (`skipInstant` برای INPUT/TEXTAREA همیشه true؛ `v63unwrapInstant` ردیف‌ها و دکمه‌ها را باز می‌کند). شمار زنده: ۰ دکمه / ۰ ردیف.
3. تب پزشکان (و داروخانه‌ها): گرید اجباری ۳ ستونه (`#formDoctor > .form-grid`)؛ `form-grid-sized` و `col-place-under` از فیلدهای غیرفول‌عرض برداشته شد تا فیلدهای یک سطر کنار هم بمانند.
4. تب سفارشات — فیلد نام داروخانه: با فوکوس/کلیک همه داروخانه‌های ثبت‌شده در تب داروخانه‌ها به‌صورت کارت زیرمجموعه نمایش داده می‌شوند؛ با تایپ فیلتر می‌شوند. محدودیت v60 (حداقل ۲ حرف / فقط هم‌نام) بازنویسی شد.
5. راستی‌آزمایی زنده: نام‌ها list=- combo=N row=N؛ instant-add=0؛ doctor grid بدون under؛ ۲ داروخانه نمونه در سفارشات روی فوکوس ظاهر شد.
6. نسخه 11.63.0؛ ۸۴/۸۴ تست سبز.

## تحویل نوبت ۸۳ — نسخه ۱۱.۶۴.۰: قفل اندازه/جای فیلدها؛ نام بدون زیرمجموعه؛ جستجوی لحظه‌ای سفارشات

1. علت جابه‌جایی/تغییر اندازه: `paintFieldBox` در هر رفرش عرض ثابت (مثلاً 260px) و `form-grid-sized` اعمال می‌کرد. از این نسخه اندازه/جا فقط با نیت صریح طراح مدیر عوض می‌شود؛ در رفرش معمولی استایل اینلاین پاک و گرید ۳ستونه قفل است.
2. علت زیرمجموعه نام داروخانه/پزشک: `setupInstantAdd` هنوز datalist به این دو فیلد می‌چسباند. حذف شد + ناظر Mutation برای کندن `list` اگر دوباره اضافه شود.
3. سفارشات: با تایپ لحظه‌ای، کادر «این داروخانه قبلاً ثبت شده» داروخانه‌های مشابه را با آدرس کامل نشان می‌دهد؛ انتخاب، همه فیلدها را جایگذاری می‌کند.
4. تأیید زنده: نام‌ها list=-؛ عرض اینلاین پزشک خالی؛ تایپ «شفا» دو کارت با آدرس؛ انتخاب → استان تهران و آدرس آزادی.

## تحویل نوبت ۸۴ — نسخه ۱۱.۶۵.۰: فیلدهای داروخانه در سفارشات + اطلاعات یکپارچه بین همه دستگاه‌ها

1. تب سفارشات حالا همان فیلدهای داروخانه را دارد: تلفن ثابت، مسئول سفارش، همراه مسئول، پلاک، طبقه، درصدی، عرض/طول جغرافیایی. با انتخاب داروخانه ثبت‌شده همه جایگذاری می‌شوند و در ذخیره سفارش می‌مانند.
2. علت اختلاف اطلاعات بین سیستم/گوشی: هر مرورگر localStorage خودش را مرجع می‌دانست و سرور را فقط وقتی خالی بود می‌خواند؛ ذخیره هم کل فایل سرور را بازنویسی می‌کرد.
3. از این نسخه سرور منبع واحد است: GET همیشه ادغام می‌شود، POST ادغام رکوردی (union + برنده تازه‌تر) می‌کند نه جایگزینی کور، و هر ذخیره ظرف ۴۰۰ms به سرور می‌رود. دستگاه جدید همان داروخانه‌ها/سفارشات/فایل‌های واردشده را می‌بیند.

## تحویل نوبت ۸۵ — نسخه ۱۱.۶۶.۰: تب تعریف مسیر نمایندگان + چک‌باکس استان/شهر/منطقه + تارگت فروش هرپخش

1. تب جدید «تعریف مسیر نمایندگان» و انتقال کادر مسیرها از تارگت فروش به این تب.
2. استان/شهر/منطقه با چک‌باکس کنار هر گزینه؛ انتخاب چندتایی؛ مناطق به‌ترتیب عددی پشت‌سرهم.
3. جدول پایین کادر: مسیر هر نماینده به‌تفکیک استان/شهر/منطقه.
4. تب جدید «تارگت فروش هرپخش» مانند تارگت نمایندگان، ریالی و به‌تفکیک کالا برای دایا/شفاآراد/تیوان/مشاطب.
5. تغییر نام تب «تارگت فروش» به «تارگت فروش نمایندگان».

## تحویل نوبت ۸۶ — فرمت ZIP استاندارد ویندوز (نسخه برنامه بدون تغییر: 11.66.0)

1. گزارش کاربر: «فایل زیپ با فرمت دیگری ظاهر شده».
2. علت شناخته‌شده: پنل پیش‌نمایش Arena محتوای ZIP را سفید/باینری نشان می‌دهد؛ زیپ قبلی create_system=Unix داشت.
3. ZIP دوباره با PKZIP ویندوز (create_system=0، archive bit، بدون extra Unix) ساخته شد.
4. سرور دانلود پورت 8000 صفحه فارسی با دکمه attachment، HEAD، و مسیر مستقیم `/namayandeelmi-v11.66.0.zip` گرفت.
5. نسخه برنامه bump نشد (قانون ۸۹/۹۱ — بدون تغییر کد CRM).

## تحویل نوبت ۸۷ — نسخه ۱۱.۶۷.۰: عرض/ارتفاع فیلد + ویرایش/حذف تارگت + یکپارچگی زنده سرور

1. عرض و ارتفاع در تب ستون‌ها و کالاها واقعاً روی فیلد اعمال می‌شود (بدون پیش‌فرض ۲۶۰ و بدون پاک‌شدن توسط قفل چیدمان).
2. در نمایش تارگت فروش نمایندگان، تارگت هرپخش و مسیر نمایندگان، روبروی هر سطر کلید ویرایش و حذف آمد.
3. اطلاعات همه دستگاه‌ها از سرور یکی می‌شود: ادغام با زمان تازه‌تر، پاسخ POST اعمال می‌شود، و هر ۸ ثانیه از سرور خوانده می‌شود.

## تحویل نوبت ۸۸ — نسخه ۱۱.۶۸.۰: ویرایش/حذف پایدار تارگت + فاصله میلی‌متری و شماره سطر

1. کلیدهای ویرایش و حذف تارگت/مسیر کار می‌کنند: بارگذاری در فرم تعریف و حذف واقعی با تأیید.
2. جدول عملیات بیرون از جدولی است که با ذخیره تارگت بازنویسی می‌شود؛ دکمه‌ها بعد از تعریف تارگت جدید پنهان نمی‌شوند.
3. در تب ستون‌ها و کالاها کنار عرض/ارتفاع سه مورد اضافه شد: فاصله نسبت به فیلد قبلی (میلی‌متر)، فاصله نسبت به فیلد بعدی (میلی‌متر)، و شماره سطر هر فیلد.

## تحویل نوبت ۸۹ — نسخه ۱۱.۶۹.۰: یکپارچگی سرور + حذف پایدار + سرعت + فاصله/سطر زنده

1. اطلاعات همه دستگاه‌ها از سرور یکی می‌شود؛ رکوردهای تکراری (هم‌نام) ادغام می‌شوند و حذف دیگر از سرور برنمی‌گردد.
2. هنگام باز شدن، برنامه اول از سرور می‌خواند و دیگر هر ۸ ثانیه کل صفحه را از نو رسم نمی‌کند.
3. کلیدهای ویرایش/حذف تارگت و مسیر واقعاً فرم را پر می‌کنند یا رکورد را با tombstone پاک می‌کنند.
4. فاصله قبلی/بعدی (میلی‌متر) و شماره سطر بعد از انتخاب فیلد بلافاصله روی فرم اعمال می‌شود.

## تحویل نوبت ۹۰ — نسخه ۱۱.۷۰.۰: قطع حلقه نوسازی + فقط همین دستگاه

1. صفحه «در حال دریافت نسخه جدید» دیگر حلقه نمی‌زند.
2. اطلاعات سیستم‌های دیگر روی سرور جایگزین/نادیده می‌شود؛ فقط اطلاعات دستگاهی که این نسخه را اول باز کند روی سرور می‌ماند.

## تحویل نوبت ۹۱ — نسخه ۱۱.۷۱.۰: اسکریپت یک‌بار + ۴۰۴ واقعی + مبنای همین دستگاه

1. crm-data/crm-app دیگر دوبار لود نمی‌شوند.
2. فایل گمشده login.html با Clear-Site-Data برنمی‌گرداند.
3. باز شدن این نسخه از همین دستگاه، سرور را فقط با داده همین سیستم جایگزین می‌کند.

## تحویل نوبت ۹۲ — نسخه ۱۱.۷۲.۰: سرور اول + حذف فلش تعداد + ویرایش/حذف پایدار + تارگت پخش محقق/مانده

1. باز شدن برنامه دیگر اول دادهٔ سیستم‌های دیگر را نشان نمی‌دهد: بوت سرور‌اول (`__v72boot`) کل state سرور را بدون ادغام می‌گیرد و همان لحظه لیست داروخانه/پزشک/سفارش را دوباره رسم می‌کند. تعویض تب هم لیست را تازه می‌کند.
2. فلش کم/زیاد فیلدهای تعداد کالا/جایزه/تارگت حذف شد؛ اسکرول و کلیدهای بالا/پایین دیگر عدد را عوض نمی‌کنند.
3. ویرایش و حذف در سه تب تارگت نمایندگان، مسیر نمایندگان و تارگت هر پخش با یک handler در capture و `stopImmediatePropagation` کار می‌کند؛ پیغام‌های عجیب روی‌هم‌رفته قطع شد.
4. کادر «نمایش تارگت‌های ثبت‌شده» حذف/مخفی شد. کلید ویرایش و حذف روبروی هر سطر در همان کادر می‌ماند.
5. تب تارگت فروش هر پخش: ستون محقق‌شده و مانده تارگت از فایل‌های هر پخش (`pharmacyRows`)؛ کادر جمع تارگت همه پخش‌ها؛ تارگت هر پخش در کادر جداگانه خودش.


## تحویل نوبت ۹۳ — نسخه ۱۱.۷۳.۰: یک state زنده + پرده تا سرور + ذخیره پایدار تارگت پخش

1. باز شدن برنامه دیگر دادهٔ سیستم‌های دیگر را نشان نمی‌دهد: `window.state` همان شیء زنده crm-app است؛ پرده تا آمدن `/api/state?__v73boot=` می‌ماند؛ adopt درجا localStorage را با دادهٔ همین سیستم جایگزین می‌کند.
2. کلیدهای ویرایش/حذف تارگت نمایندگان، مسیر نمایندگان و تارگت هر پخش روی همان سطر می‌مانند؛ کارت‌های قدیمی «نمایش تارگت‌های ثبت‌شده» حذف می‌شوند نه پنهان لحظه‌ای.
3. ذخیره تارگت هر پخش بعد از ویرایش یا حذف+ورود دوباره تعداد واقعاً می‌ماند: تطبیق ماه با `monthName` یا `ماه/سال`، به‌روزرسانی همان id، پاک tombstone کلید طبیعی، POST با replace=1.

## تحویل نوبت ۹۴ — نسخه ۱۱.۷۴.۰: گزارش فروش مشاطب مانند دایا و شفاآراد

1. فایل داروخانه مشاطب: ستون ۱۱ فروش تعدادی، ستون ۱۲ تعداد جایزه، ستون ۱۰ شناسه داروخانه.
2. فروش ریالی پخش و داروخانه از ضرب تعداد در قیمت‌های ثبت‌شده برنامه.
3. درصد جایزه به فروش و درصد فروش به‌تفکیک کالا از همان تعدادها.
4. تطبیق کد کالا: 1001=186101 تا 1007=186107.

## تحویل نوبت ۹۵ — نسخه ۱۱.۷۵.۰: طراح ستون‌ها زنده + حذف Autofill + دیتابیس پخش + تاریخ مشاطب + تکراری مکان

1. تغییر شماره ترتیب فرم/لیست، عرض، ارتفاع، فاصله میلی‌متری، شماره سطر و جای فیلد واقعاً روی فرم و لیست اعمال می‌شود.
2. کادر Autofill and passwords روی استان/شهر/منطقه حذف شد؛ پرش مداوم شهرها با قطع interval فیلتر مسیر رفع شد.
3. در تب دیتابیس پخش‌ها روبروی دیتابیس داروخانه و موجودی فعلی کلید ویرایش و حذف آمد.
4. آخرین تاریخ داروخانه مشاطب از ستون شماره ۳ خوانده می‌شود.
5. نام داروخانه و پزشک بدون زیرمجموعه؛ ثبت جدید با نام+استان+شهر+منطقه(+آدرس) تکراری کاملاً مسدود است.

## تحویل نوبت ۹۶ — نسخه ۱۱.۷۸.۰: قیمت مصرف‌کننده، مارژین قانونی، طراح زنده، اعمال لحظه‌ای

1. در تب ستون‌ها و کالاها فیلد و ستون «قیمت مصرف‌کننده» اضافه شد و با ذخیره کالا در همه تب‌های کالا (سفارش، تارگت، قیمت‌گذاری) همان لحظه ظاهر می‌شود.
2. با سررسید تاریخ اعمال در تب قیمت‌گذاری، قیمت پخش/داروخانه/مصرف‌کننده در کاتالوگ کالا هم عوض می‌شود.
3. جلو هر سطر قیمت‌گذاری کلید ویرایش است؛ در حالت ویرایش درصد ارزش افزوده در هر دو کادر قابل تغییر است.
4. ستون ۳ و ۵ با قانون مارژین محاسبه می‌شود: (قیمت بالاتر − قیمت پایین‌تر) ÷ قیمت بالاتر؛ نمونه 3,358,080 و 3,816,000 = ۱۲٪. برگشت قیمت از مصرف‌کننده با ضرب (1 − مارژین/100).
5. طراح ستون (ترتیب فرم/لیست، عرض، ارتفاع، فاصله میلی‌متری، شماره سطر، جای فیلد) پس از انتخاب فیلد زنده اعمال می‌شود و در سفارشات گروه‌های تو در تو جابه‌جا نمی‌شوند.
6. ذخیره و ثبت بدون رفرش دستی در نمای فعال اعمال می‌شود.

## تحویل نوبت ۹۷ — نسخه ۱۱.۷۹.۰: فقط همین دستگاه + یک ستون کد کالا

1. اطلاعات سیستم‌های دیگر دیگر روی این دستگاه نمی‌نشیند: snapshot محلی قبل از adopt سرور برمی‌گردد و با replace=1 سرور را از همین دستگاه پر می‌کند.
2. جدول کالا فقط یک ستون «کد کالا» دارد؛ ستون تکراری داخل نام کالا حذف شد.
3. قیمت مصرف‌کننده در مسیر ذخیره v20 هم ذخیره می‌شود.
4. قانون جدید: بلوک پاورشل پایان هر چت شامل پاکسازی فایل‌ها و فولدرهای اضافی است.

## تحویل نوبت ۹۸ — نسخه ۱۱.۸۰.۰: قفل داده قبلی + ملاک سرور + مصرف‌کننده از درصد افزایش

1. ورود داده نسخه/سیستم قبلی قفل شد: POST بدون `_dataGen=11.80.0` نادیده گرفته می‌شود و با سرور ادغام نمی‌گردد.
2. بوت فقط GET سرور را ملاک می‌گیرد؛ snapshot محلی دیگر روی سرور بازنویسی نمی‌شود.
3. رکوردهای نمونه قدیمی (داروخانه عرفانی/رازی/عقبایی و مشابه) از کد پیش‌فرض و از state حذف شدند.
4. در کادر قیمت‌های جدید، ستون مصرف‌کننده = مصرف‌کننده فعلی × (۱ + درصد افزایش) است و با قیمت داروخانه یکی گرفته نمی‌شود.

## تحویل نوبت ۹۹ — نسخه ۱۱.۸۱.۰: پاکسازی قطعی داده قبلی + مصرف‌کننده از فرمول افزایش

1. با ارتقا به ۱۱.۸۱ لیست داروخانه/پزشک/سفارش و سایر داده‌های عملیاتی نسل قبل خالی می‌شود؛ کالا و تنظیمات مدیر می‌ماند.
2. POST نسخه قدیمی رد می‌شود. ملاک فقط state سرور با `_dataGen=11.81.0` است.
3. ستون مصرف‌کننده کادر قیمت جدید از مصرف‌کننده فعلی × (۱+درصد افزایش) پر می‌شود و از قیمت داروخانه کپی نمی‌شود.

## تحویل نوبت ۱۰۰ — نسخه ۱۱.۸۲.۰: اعمال واقعی ترتیب طراح ستون‌ها

1. شماره ترتیب فرم در تب ستون‌ها همان جای فیلد در تب اصلی است (مثلاً نام داروخانه سفارشات اگر ۲ باشد در فرم سفارشات هم دوم است).
2. عرض، ارتفاع، فاصله میلی‌متری، شماره سطر و جای فیلد از meta روی فرم زنده اعمال می‌شود.
3. ترتیب لیست از listOrder پیروی می‌کند و ستون نماینده دیگر اجباری در جایگاه ۲ نیست.

## تحویل نوبت ۱۰۱ — نسخه ۱۱.۸۳.۰: توقف پرش تایپ + موبایل ایران

1. هنگام تایپ در فرم داروخانه/پزشک/سفارش دیگر DOM جابه‌جا نمی‌شود.
2. فایل‌های download-v11.68/69/70.html از لیست رسمی حذف شدند و در پاکسازی اضافه حذف می‌شوند؛ روی اجرای برنامه اثر ندارند.
3. کاشی نقشه از `/api/tiles` روی خود Render می‌آید تا OSM مستقیم روی همراه اول/ایرانسل بلاک نشود. ورود روی موبایل با localStorage پایدار است. Service Worker با مهلت ۸ ثانیه و کش کار می‌کند.

## تحویل نوبت ۱۰۲ — نسخه ۱۱.۸۴.۰: داشبورد حرفه‌ای + توقف پرش تایپ + قفل ترتیب سفارشات

1. نمودار داشبورد از داده واقعی است و کاربران نمونه قدیمی را نشان نمی‌دهد. فیلتر سال، ماه، استان، شهر، منطقه اضافه شد.
2. هنگام فوکوس فیلد، restoreDomFieldOrder و جابه‌جایی DOM اجرا نمی‌شود.
3. ترتیب سفارشات با CSS order از طراح می‌ماند و ناظر دیگر فرم را به HTML قدیمی برنمی‌گرداند.

## تحویل نوبت ۱۰۳ — نسخه ۱۱.۸۵.۰: مسیر یک عملیات + جستجوی جغرافیا + داشبورد خوانا

1. در تب تعریف مسیر فقط یک جفت ویرایش/حذف می‌ماند.
2. جستجوی لحظه‌ای استان/شهر/منطقه و چک‌باکس همه استان‌ها/شهرها/مناطق بالای هر لیست.
3. داشبورد نام نماینده را کامل (بدون بریدن به آقا/خانم) و ماه را کامل نشان می‌دهد و چیدمان دو ستونه است.

## تحویل نوبت ۱۰۴ — نسخه ۱۱.۸۶.۰: توقف حذف داروخانه کاربر + مسیر + موبایل

1. دیگر هیچ ارتقایی لیست داروخانه/پزشک/سفارش را خالی نمی‌کند. فقط رکوردهای نمونه قدیمی حذف می‌شوند. اگر لیست خالی باشد از آرشیو localStorage بازیابی می‌شود.
2. ویرایش/حذف مسیر با کلیک پایدار و بدون بازنویسی دکمه.
3. شماره نسخه روی ورود و هدر؛ یک‌بار پاک‌سازی کش موبایل هنگام عوض شدن نسخه.

## تحویل نوبت ۱۰۸ — نسخه ۱۱.۹۰.۰: موبایل، تخصص، حریم نماینده، رصد تردد، کلید استاندارد

1. در گوشی صفحه سفید رفع شد؛ نوار افقی تب‌ها مخفی است و منوی همبرگری تب‌ها را نشان می‌دهد.
2. فیلد تخصص پزشک با فهرست کامل تخصص‌های پزشکی پر شد.
3. نماینده علمی فقط اطلاعات و فعالیت خودش را می‌بیند؛ مدیر همه را می‌بیند.
4. لیست رصد تردد از ورود اول مرتب است و نیاز به پاک‌کردن جستجو ندارد.
5. کلید حذف همه جا قرمز با سطل است؛ ویرایش، ذخیره و پاک‌کردن یکسان شدند.
